# Abyss Shooter P1「核爽」—— core 纯逻辑实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现深渊射手的纯逻辑核心（5 列加农炮 / 连通块合并结算 / 弹药放大 / 向上重力 / 温柔刷行 / 唯一失败 / 连锁倍率 + 一套鱼梯数据），全部用 node 单测 + 蒙特卡洛验证，先坐实「射鱼-连锁-被顶爆」这套核心规则可跑、确定、可调平衡。

**Architecture:** 纯逻辑层，无 DOM，双导出（`if(module.exports)...else 全局`），同 snake/mines 的 core 模式。单一可变状态 `s`，`shoot(s,col)` 是唯一交互入口，每次调用清空重填 `s.events`。棋盘用**每列一个数组、index 0 = 顶（远离玩家）、末尾 = 底（玩家侧/死线）**，顶锚定、密实；连通块合并按 DESIGN §1 的确定性单趟语义。本计划**只做 core + tiles + 测试**，不含 render/main/DOM（下一个计划做可玩壳）。

**Tech Stack:** 纯 JavaScript（无 TS/框架/bundler），node 内建 `assert` 跑测试，引擎 `PRNG.create(seed)`（mulberry32）做可复现随机。参考：`engine/README.md`、`engine/prng.js`、`games/snake/js/core.js`、`games/snake/tests/test-core.js`、root 与 snake 的 `CLAUDE.md`。

**关键约定（来自 DESIGN.md，实现时是硬规则）：**
- 棋盘 `s.board`：长度 `cols` 的数组，`s.board[c]` 是该列的栈，**index 0 = 顶、末尾 = 底（玩家侧）**。值为 2 的幂（>0）；密实（无内部空洞，空洞只在 resolve 中途瞬时出现）。
- **连通块合并（确定性）**：一整个四邻相连同值块（≥2）→ 塌成 1 个 ×2，落在**最低（index 最大）、再最左（c 最小）**锚点；单趟找出所有块→同趟结算→一次向上重力压实→重扫，直到无块。**连锁倍率 = 迭代轮数**。
- **弹药放大**：`genAmmo` 在 `[最小档 .. 最小档×4]` 里取（防大鱼变死墙），`AMMO_WINDOW` 为可调常量。
- **向上重力**：合并腾空后每列去零、向 index 0 压实。
- **温柔刷行**：每 `SPAWN_EVERY` 发在顶部插一整行小鱼下压。
- **唯一失败**：任意列高度 > `rows` 即死。
- **禁 `Date.now()`**（core 纯逻辑，随机全走 `s.rand`）。

---

## 文件结构

- 新建 `games/abyssshoot/js/tiles.js` —— 鱼梯数据（数字→鱼）+ 助手（`TILES`/`tierOf`/`fishOf`/`fmt`/`MAX_TILE_VALUE`）。双导出。
- 新建 `games/abyssshoot/js/core.js` —— 纯状态机（`createGame`/`shoot` + 内部 `resolve`/`findComponents`/`gravityUp`/`spawnRow`/`genAmmo` 等）。双导出。
- 新建 `games/abyssshoot/tests/test-tiles.js` —— tiles 单测。
- 新建 `games/abyssshoot/tests/test-core.js` —— core 单测（含连通块确定性、重力、弹药区间、刷行、失败、连锁倍率）。
- 新建 `games/abyssshoot/tests/test-sim.js` —— 蒙特卡洛/冒烟（跑 N 局随机选列，验不变量 + 局长分布 + resolve 不死循环）。
- 修改 `package.json`（root）—— 加 `test:abyss` 脚本并挂进 `test`。

---

## Task 1: tiles.js —— 鱼梯数据与助手

**Files:**
- Create: `games/abyssshoot/js/tiles.js`
- Test: `games/abyssshoot/tests/test-tiles.js`

- [ ] **Step 1: 写失败测试**

Create `games/abyssshoot/tests/test-tiles.js`:

```javascript
const assert = require('assert');
const Tiles = require('../js/tiles.js');

// TILES 是按 2 的幂递增的数组,从 2 起
assert(Array.isArray(Tiles.TILES) && Tiles.TILES.length >= 12, 'TILES 至少 12 档');
assert.strictEqual(Tiles.TILES[0].v, 2, '第 0 档是 2');
for (let i = 0; i < Tiles.TILES.length; i++) {
  assert.strictEqual(Tiles.TILES[i].v, 2 ** (i + 1), `第 ${i} 档应为 2^${i + 1}`);
  assert(typeof Tiles.TILES[i].fish === 'string' && Tiles.TILES[i].fish.length > 0, '每档有 fish id');
}

// tierOf: 值→档位下标
assert.strictEqual(Tiles.tierOf(2), 0);
assert.strictEqual(Tiles.tierOf(64), 5);
assert.strictEqual(Tiles.tierOf(3), -1, '非梯值返回 -1');

// fishOf: 值→鱼 id
assert.strictEqual(Tiles.fishOf(2), Tiles.TILES[0].fish);
assert.strictEqual(Tiles.fishOf(999), null, '非梯值返回 null');

// fmt: 巨数缩写
assert.strictEqual(Tiles.fmt(8192), '8192');
assert.strictEqual(Tiles.fmt(1048576), '1M');
assert.strictEqual(Tiles.fmt(2097152), '2M');

// MAX_TILE_VALUE = 最高档值
assert.strictEqual(Tiles.MAX_TILE_VALUE, Tiles.TILES[Tiles.TILES.length - 1].v);

console.log('test-tiles OK');
```

- [ ] **Step 2: 跑测试看它失败**

Run: `node games/abyssshoot/tests/test-tiles.js`
Expected: FAIL — `Cannot find module '../js/tiles.js'`

- [ ] **Step 3: 写最小实现**

Create `games/abyssshoot/js/tiles.js`:

```javascript
// tiles.js — 数字→鱼 梯子数据 + 助手(纯数据,双导出)
// 鱼 id 均来自 fishId/assets/fish/cute/ 已确认存在的物种;P1 用这 13 档,后续可扩。
const TILES = [
  { v: 2,     fish: 'clownfish' },
  { v: 4,     fish: 'blenny' },
  { v: 8,     fish: 'butterflyfish' },
  { v: 16,    fish: 'angelfish' },
  { v: 32,    fish: 'blackspottedpuffer' },
  { v: 64,    fish: 'barracuda' },
  { v: 128,   fish: 'blacktipreefshark' },
  { v: 256,   fish: 'anglerfish' },
  { v: 512,   fish: 'barreleye' },
  { v: 1024,  fish: 'coelacanth' },
  { v: 2048,  fish: 'greatwhiteshark' },
  { v: 4096,  fish: 'whaleshark' },
  { v: 8192,  fish: 'belugawhale' },
];
const MAX_TILE_VALUE = TILES[TILES.length - 1].v;

function tierOf(v) {
  for (let i = 0; i < TILES.length; i++) if (TILES[i].v === v) return i;
  return -1;
}
function fishOf(v) {
  const t = tierOf(v);
  return t < 0 ? null : TILES[t].fish;
}
function fmt(v) {
  if (v >= 1e9) return (v / 1e9) + 'B';
  if (v >= 1e6) return (v / 1e6) + 'M';
  return String(v);
}

// 双导出:node 走 module.exports;浏览器靠顶层 const Tiles 当全局(同一词法环境后续脚本可见,同 snake/fruits)
const Tiles = { TILES, MAX_TILE_VALUE, tierOf, fishOf, fmt };
if (typeof module !== 'undefined' && module.exports) module.exports = Tiles;
```

- [ ] **Step 4: 跑测试看它通过**

Run: `node games/abyssshoot/tests/test-tiles.js`
Expected: PASS — `test-tiles OK`

- [ ] **Step 5: 提交**

```bash
git add games/abyssshoot/js/tiles.js games/abyssshoot/tests/test-tiles.js
git commit -m "feat(abyssshoot): tiles 鱼梯数据+助手(tierOf/fishOf/fmt)"
```

---

## Task 2: core createGame —— 棋盘模型与初始不变量

**Files:**
- Create: `games/abyssshoot/js/core.js`
- Test: `games/abyssshoot/tests/test-core.js`

- [ ] **Step 1: 写失败测试**

Create `games/abyssshoot/tests/test-core.js`:

```javascript
const assert = require('assert');
const Core = require('../js/core.js');

// --- 初始状态 ---
let s = Core.createGame({ seed: 1 });
assert.strictEqual(s.cols, 5);
assert.strictEqual(s.rows, 9);
assert.strictEqual(s.board.length, 5, '5 列');
assert(s.board.every(col => Array.isArray(col) && col.length === 0), '开局空盘');
assert.strictEqual(s.score, 0);
assert.strictEqual(s.maxTile, 0);
assert(!s.dead);
assert(Number.isInteger(Math.log2(s.ammo)) && s.ammo >= 2, 'ammo 是 >=2 的 2 的幂');
assert.strictEqual(s.queue.length, 3, '预览 3 发');
assert(s.queue.every(v => Number.isInteger(Math.log2(v)) && v >= 2));

// --- 同种子可复现 ---
const a = Core.createGame({ seed: 42 });
const b = Core.createGame({ seed: 42 });
assert.strictEqual(a.ammo, b.ammo);
assert.deepStrictEqual(a.queue, b.queue);

console.log('test-core: createGame OK');
```

- [ ] **Step 2: 跑测试看它失败**

Run: `node games/abyssshoot/tests/test-core.js`
Expected: FAIL — `Cannot find module '../js/core.js'`

- [ ] **Step 3: 写最小实现**

Create `games/abyssshoot/js/core.js`:

```javascript
// core.js — 纯游戏状态机(无 DOM,双导出)。棋盘 = 每列一个栈,index 0=顶、末尾=底(玩家侧)。
const PRNG_ = (typeof module !== 'undefined' && module.exports)
  ? require('../../../engine/prng.js') : PRNG;

const PREVIEW = 3;       // 弹药预览发数
const AMMO_WINDOW = 3;   // 弹药档窗:base * 2^(0..AMMO_WINDOW-1) = base, ×2, ×4 (可调)
const SPAWN_EVERY = 6;   // 每 N 发顶部刷一整行(可调)
const TILE_MIN = 2;      // 最小档

function smallestTile(s) {
  let m = Infinity;
  for (let c = 0; c < s.cols; c++) for (const v of s.board[c]) if (v < m) m = v;
  return m === Infinity ? TILE_MIN : m;
}
function genAmmo(s) {
  const base = smallestTile(s);
  const e = Math.floor(s.rand() * AMMO_WINDOW);   // 0..AMMO_WINDOW-1
  return base * Math.pow(2, e);
}

function createGame(opts = {}) {
  const cols = opts.cols || 5, rows = opts.rows || 9;
  const seed = opts.seed == null ? 1 : opts.seed;
  const s = {
    cols, rows, seed,
    rand: PRNG_.create(seed),
    board: Array.from({ length: cols }, () => []),
    score: 0, maxTile: 0,
    shots: 0, shotsSinceSpawn: 0,
    dead: false, events: [],
    ammo: 0, queue: [],
  };
  s.ammo = genAmmo(s);
  for (let k = 0; k < PREVIEW; k++) s.queue.push(genAmmo(s));
  return s;
}

// 双导出:node 走 module.exports;浏览器靠顶层 const Core 当全局(同 snake core.js)
const Core = { createGame, genAmmo, smallestTile,
  PREVIEW, AMMO_WINDOW, SPAWN_EVERY, TILE_MIN };
if (typeof module !== 'undefined' && module.exports) module.exports = Core;
```

- [ ] **Step 4: 跑测试看它通过**

Run: `node games/abyssshoot/tests/test-core.js`
Expected: PASS — `test-core: createGame OK`

- [ ] **Step 5: 提交**

```bash
git add games/abyssshoot/js/core.js games/abyssshoot/tests/test-core.js
git commit -m "feat(abyssshoot): core createGame 棋盘模型+弹药生成"
```

---

## Task 3: gravityUp —— 向上贴顶重力（去零压实）

**Files:**
- Modify: `games/abyssshoot/js/core.js`
- Test: `games/abyssshoot/tests/test-core.js`

- [ ] **Step 1: 追加失败测试**

Append to `games/abyssshoot/tests/test-core.js`:

```javascript
// --- gravityUp: 每列去零、保持顺序、向 index 0 压实 ---
s = Core.createGame({ seed: 1 });
s.board = [[2, 0, 4, 0], [0, 0, 8], [], [16], [0]];
Core.gravityUp(s);
assert.deepStrictEqual(s.board[0], [2, 4], '列0 去零保序');
assert.deepStrictEqual(s.board[1], [8], '列1 去零');
assert.deepStrictEqual(s.board[2], [], '列2 空');
assert.deepStrictEqual(s.board[3], [16], '列3 不变');
assert.deepStrictEqual(s.board[4], [], '列4 全零→空');
console.log('test-core: gravityUp OK');
```

- [ ] **Step 2: 跑测试看它失败**

Run: `node games/abyssshoot/tests/test-core.js`
Expected: FAIL — `Core.gravityUp is not a function`

- [ ] **Step 3: 实现**

In `games/abyssshoot/js/core.js`, add before the `const Core` line:

```javascript
function gravityUp(s) {
  for (let c = 0; c < s.cols; c++) s.board[c] = s.board[c].filter(v => v > 0);
}
```

And update the `const Core` object (add `gravityUp`):

```javascript
const Core = { createGame, genAmmo, smallestTile, gravityUp,
  PREVIEW, AMMO_WINDOW, SPAWN_EVERY, TILE_MIN };
```

- [ ] **Step 4: 跑测试看它通过**

Run: `node games/abyssshoot/tests/test-core.js`
Expected: PASS — 两行 OK

- [ ] **Step 5: 提交**

```bash
git add games/abyssshoot/js/core.js games/abyssshoot/tests/test-core.js
git commit -m "feat(abyssshoot): gravityUp 向上贴顶重力"
```

---

## Task 4: findComponents —— 连通块查找（确定性核心）

**Files:**
- Modify: `games/abyssshoot/js/core.js`
- Test: `games/abyssshoot/tests/test-core.js`

adjacency = 同列 index±1 + 相邻列同 index（顶锚定密实数组里 index 即视觉行，故横向 = 相邻列同下标）。只返回 size≥2 的块；锚点 = index 最大（最低）、再 c 最小（最左）。

- [ ] **Step 1: 追加失败测试**

Append to `games/abyssshoot/tests/test-core.js`:

```javascript
// --- findComponents: 竖向对 ---
s = Core.createGame({ seed: 1 });
s.board = [[4, 4], [], [], [], []];   // 列0 两个 4 相邻(index0,1)
let comps = Core.findComponents(s);
assert.strictEqual(comps.length, 1, '一个竖向连通块');
assert.strictEqual(comps[0].value, 4);
assert.strictEqual(comps[0].cells.length, 2);
assert.deepStrictEqual(comps[0].anchor, { c: 0, i: 1 }, '锚点取 index 最大');

// --- 横向对:相邻列同 index ---
s = Core.createGame({ seed: 1 });
s.board = [[8], [8], [], [], []];     // 列0/列1 的 index0 都是 8
comps = Core.findComponents(s);
assert.strictEqual(comps.length, 1, '一个横向连通块');
assert.deepStrictEqual(comps[0].anchor, { c: 0, i: 0 }, '同 index 时锚点取最左 c');

// --- L 形 4 连块 ---
s = Core.createGame({ seed: 1 });
s.board = [[2, 2], [2], [], [], []];  // (0,0)(0,1)(1,0) 都是 2;(1,0)与(0,0)横邻,(0,1)与(0,0)竖邻
comps = Core.findComponents(s);
assert.strictEqual(comps.length, 1);
assert.strictEqual(comps[0].cells.length, 3, 'L 形 3 连');
assert.deepStrictEqual(comps[0].anchor, { c: 0, i: 1 }, '最低最左');

// --- 单个不成块;不同值不连 ---
s = Core.createGame({ seed: 1 });
s.board = [[2, 4], [8], [], [], []];
assert.strictEqual(Core.findComponents(s).length, 0, '无 ≥2 同值相邻');

// --- 两个分离的同值块各自计 ---
s = Core.createGame({ seed: 1 });
s.board = [[4, 4], [], [4, 4], [], []];   // 列0 一对、列2 一对,列1 空隔开
assert.strictEqual(Core.findComponents(s).length, 2, '分离两块');
console.log('test-core: findComponents OK');
```

- [ ] **Step 2: 跑测试看它失败**

Run: `node games/abyssshoot/tests/test-core.js`
Expected: FAIL — `Core.findComponents is not a function`

- [ ] **Step 3: 实现**

In `games/abyssshoot/js/core.js`, add before `const Core`:

```javascript
function findComponents(s) {
  const comps = [];
  const seen = s.board.map(col => col.map(() => false));
  for (let c = 0; c < s.cols; c++) {
    for (let i = 0; i < s.board[c].length; i++) {
      if (seen[c][i]) continue;
      const v = s.board[c][i];
      seen[c][i] = true;
      if (v <= 0) continue;
      const cells = [{ c, i }];
      const stack = [{ c, i }];
      while (stack.length) {
        const cur = stack.pop();
        const nb = [
          { c: cur.c, i: cur.i - 1 }, { c: cur.c, i: cur.i + 1 },
          { c: cur.c - 1, i: cur.i }, { c: cur.c + 1, i: cur.i },
        ];
        for (const n of nb) {
          if (n.c < 0 || n.c >= s.cols) continue;
          if (n.i < 0 || n.i >= s.board[n.c].length) continue;
          if (seen[n.c][n.i]) continue;
          if (s.board[n.c][n.i] !== v) continue;
          seen[n.c][n.i] = true;
          cells.push(n); stack.push(n);
        }
      }
      if (cells.length >= 2) {
        let anchor = cells[0];
        for (const cell of cells)
          if (cell.i > anchor.i || (cell.i === anchor.i && cell.c < anchor.c)) anchor = cell;
        comps.push({ value: v, cells, anchor });
      }
    }
  }
  return comps;
}
```

Update the `const Core` object (add `findComponents`):

```javascript
const Core = { createGame, genAmmo, smallestTile, gravityUp, findComponents,
  PREVIEW, AMMO_WINDOW, SPAWN_EVERY, TILE_MIN };
```

- [ ] **Step 4: 跑测试看它通过**

Run: `node games/abyssshoot/tests/test-core.js`
Expected: PASS — 到 findComponents OK

- [ ] **Step 5: 提交**

```bash
git add games/abyssshoot/js/core.js games/abyssshoot/tests/test-core.js
git commit -m "feat(abyssshoot): findComponents 连通块查找(确定性锚点)"
```

---

## Task 5: resolve —— 单趟结算循环（合并+连锁倍率+计分）

**Files:**
- Modify: `games/abyssshoot/js/core.js`
- Test: `games/abyssshoot/tests/test-core.js`

DESIGN §1(b)：每轮找出所有块→同趟结算(整块塌成 1 个 ×2 落锚点、其余清零)→gravityUp→重扫,直到无块。连锁倍率=迭代轮数,`gained += 新值 * 当前轮数`。

- [ ] **Step 1: 追加失败测试**

Append to `games/abyssshoot/tests/test-core.js`:

```javascript
// --- resolve: 单块 4+4 → 8,一轮 ---
s = Core.createGame({ seed: 1 });
s.board = [[4, 4], [], [], [], []];
let r = Core.resolve(s);
assert.deepStrictEqual(s.board[0], [8], '4,4→8');
assert.strictEqual(r.chain, 1, '一轮');
assert.strictEqual(s.score, 8 * 1, 'gained=8×1');
assert.strictEqual(s.maxTile, 8);

// --- 整个 ≥2 同值连通块塌成 1 个(3 连 → 1 个 ×2) ---
s = Core.createGame({ seed: 1 });
s.board = [[2, 2], [2], [], [], []];   // L 形 3 个 2
Core.resolve(s);
const flat = s.board.flat();
assert.deepStrictEqual(flat, [4], '3 个 2 塌成 1 个 4');

// --- 连锁:2,2 顶上还有个 4 → 合出 4 再与之连锁成 8,两轮 ---
s = Core.createGame({ seed: 1 });
s.board = [[4, 2, 2], [], [], [], []];  // index0=4,index1=2,index2=2
r = Core.resolve(s);
assert.deepStrictEqual(s.board[0], [8], '2,2→4 再与顶部 4→8');
assert.strictEqual(r.chain, 2, '两轮连锁');
assert.strictEqual(s.score, 4 * 1 + 8 * 2, '第1轮4×1 + 第2轮8×2');

// --- 无块:不变 ---
s = Core.createGame({ seed: 1 });
s.board = [[2, 4], [], [], [], []];
r = Core.resolve(s);
assert.strictEqual(r.chain, 0);
assert.strictEqual(s.score, 0);

// --- newMaxFish 事件 ---
s = Core.createGame({ seed: 1 });
s.board = [[8, 8], [], [], [], []];
Core.resolve(s);
assert(s.events.some(e => e.t === 'newMaxFish' && e.v === 16), '刷新最高档发事件');
console.log('test-core: resolve OK');
```

- [ ] **Step 2: 跑测试看它失败**

Run: `node games/abyssshoot/tests/test-core.js`
Expected: FAIL — `Core.resolve is not a function`

- [ ] **Step 3: 实现**

In `games/abyssshoot/js/core.js`, add before `const Core`:

```javascript
function resolve(s) {
  let chain = 0, gained = 0, merges = 0;
  while (true) {
    const comps = findComponents(s);
    if (!comps.length) break;
    chain++;
    for (const comp of comps) {
      const nv = comp.value * 2;
      for (const cell of comp.cells) s.board[cell.c][cell.i] = 0;   // 整块清零
      s.board[comp.anchor.c][comp.anchor.i] = nv;                   // 锚点置 ×2
      gained += nv * chain;                                         // 连锁倍率
      merges++;
      if (nv > s.maxTile) { s.maxTile = nv; s.events.push({ t: 'newMaxFish', v: nv }); }
      s.events.push({ t: 'merge', v: nv, chain });
    }
    gravityUp(s);
  }
  if (chain > 1) s.events.push({ t: 'chain', n: chain });
  s.score += gained;
  return { chain, gained, merges };
}
```

Update the `const Core` object (add `resolve`):

```javascript
const Core = { createGame, genAmmo, smallestTile, gravityUp, findComponents, resolve,
  PREVIEW, AMMO_WINDOW, SPAWN_EVERY, TILE_MIN };
```

- [ ] **Step 4: 跑测试看它通过**

Run: `node games/abyssshoot/tests/test-core.js`
Expected: PASS — 到 resolve OK

- [ ] **Step 5: 提交**

```bash
git add games/abyssshoot/js/core.js games/abyssshoot/tests/test-core.js
git commit -m "feat(abyssshoot): resolve 单趟结算循环+连锁倍率+计分"
```

---

## Task 6: spawnRow —— 温柔刷行下压

**Files:**
- Modify: `games/abyssshoot/js/core.js`
- Test: `games/abyssshoot/tests/test-core.js`

顶部插一整行小鱼（每列 index 0 unshift 一个 2 或 4）。

- [ ] **Step 1: 追加失败测试**

Append to `games/abyssshoot/tests/test-core.js`:

```javascript
// --- spawnRow: 每列顶部各加一个小鱼(2 或 4),原有下移 ---
s = Core.createGame({ seed: 7 });
s.board = [[16], [32], [], [64], []];
Core.spawnRow(s);
for (let c = 0; c < 5; c++) {
  const first = s.board[c][0];
  assert(first === 2 || first === 4, `列${c} 顶部是新小鱼`);
}
assert.deepStrictEqual(s.board[0].slice(1), [16], '列0 原鱼被下移到 index1');
assert.deepStrictEqual(s.board[3].slice(1), [64], '列3 原鱼下移');
assert(s.events.some(e => e.t === 'spawn'), '发 spawn 事件');
console.log('test-core: spawnRow OK');
```

- [ ] **Step 2: 跑测试看它失败**

Run: `node games/abyssshoot/tests/test-core.js`
Expected: FAIL — `Core.spawnRow is not a function`

- [ ] **Step 3: 实现**

In `games/abyssshoot/js/core.js`, add before `const Core`:

```javascript
function spawnTile(s) {
  return TILE_MIN * Math.pow(2, Math.floor(s.rand() * 2));   // 2 或 4
}
function spawnRow(s) {
  for (let c = 0; c < s.cols; c++) s.board[c].unshift(spawnTile(s));
  s.events.push({ t: 'spawn' });
}
```

Update the `const Core` object (add `spawnRow`):

```javascript
const Core = { createGame, genAmmo, smallestTile, gravityUp, findComponents, resolve, spawnRow,
  PREVIEW, AMMO_WINDOW, SPAWN_EVERY, TILE_MIN };
```

- [ ] **Step 4: 跑测试看它通过**

Run: `node games/abyssshoot/tests/test-core.js`
Expected: PASS — 到 spawnRow OK

- [ ] **Step 5: 提交**

```bash
git add games/abyssshoot/js/core.js games/abyssshoot/tests/test-core.js
git commit -m "feat(abyssshoot): spawnRow 温柔刷行下压"
```

---

## Task 7: shoot —— 唯一交互入口（射击→结算→刷行→失败→换弹）

**Files:**
- Modify: `games/abyssshoot/js/core.js`
- Test: `games/abyssshoot/tests/test-core.js`

顺序：清 `events` → 校验 → 弹药 push 到目标列底(append) → `resolve` → 到 `SPAWN_EVERY` 则 `spawnRow`+`resolve`+清计数 → `shots++` → 失败判定(任列高度 > rows) → 未死则换弹(`ammo=queue.shift()`,`queue.push(genAmmo)`)。

- [ ] **Step 1: 追加失败测试**

Append to `games/abyssshoot/tests/test-core.js`:

```javascript
// --- shoot: 射进空列,弹药落底,换新弹 ---
s = Core.createGame({ seed: 1 });
const firstAmmo = s.ammo, firstQueue = s.queue.slice();
Core.shoot(s, 2);
assert.strictEqual(s.board[2][s.board[2].length - 1], firstAmmo, '弹药落在目标列底');
assert.strictEqual(s.ammo, firstQueue[0], '换成队列下一发');
assert.strictEqual(s.queue.length, 3, '队列仍 3 发');
assert.strictEqual(s.shots, 1);
assert(s.events.some(e => e.t === 'shoot' && e.c === 2), '发 shoot 事件');

// --- shoot 触发合并:列底同数 → 合 ---
s = Core.createGame({ seed: 1 });
s.board = [[], [], [8], [], []];
s.ammo = 8;
Core.shoot(s, 2);
assert.deepStrictEqual(s.board[2], [16], '8 射到 8 上→16');

// --- 越界/死局不炸 ---
s = Core.createGame({ seed: 1 });
Core.shoot(s, 9);          // 越界:无操作
assert.strictEqual(s.shots, 0, '越界不计');
s.dead = true;
Core.shoot(s, 0);
assert.strictEqual(s.shots, 0, '死局不动');

// --- SPAWN_EVERY 发后触发刷行 ---
s = Core.createGame({ seed: 3 });
for (let k = 0; k < Core.SPAWN_EVERY; k++) Core.shoot(s, k % 5);
assert(s.events.some(e => e.t === 'spawn'), '第 SPAWN_EVERY 发后有 spawn');
assert.strictEqual(s.shotsSinceSpawn, 0, '刷行后计数清零');

// --- 失败:把一列灌到超高 ---
// 用相邻不同值填满(否则同值会连锁合并、缩短、反而不死);ammo 也与列底不同数
s = Core.createGame({ seed: 1 });
const alt = [];
for (let i = 0; i < s.rows; i++) alt.push(i % 2 === 0 ? 2 : 4);   // 2,4,2,4,... 共 rows 个,相邻不同
s.board = [alt, [], [], [], []];
s.ammo = 8;
Core.shoot(s, 0);
assert(s.dead, '列高超 rows → 死');
assert(s.events.some(e => e.t === 'death'));
console.log('test-core: shoot OK');
```

- [ ] **Step 2: 跑测试看它失败**

Run: `node games/abyssshoot/tests/test-core.js`
Expected: FAIL — `Core.shoot is not a function`

- [ ] **Step 3: 实现**

In `games/abyssshoot/js/core.js`, add before `const Core`:

```javascript
function shoot(s, col) {
  s.events = [];
  if (s.dead) return s;
  if (col < 0 || col >= s.cols) return s;

  s.board[col].push(s.ammo);
  s.events.push({ t: 'shoot', c: col, v: s.ammo });
  resolve(s);

  if (++s.shotsSinceSpawn >= SPAWN_EVERY) {
    spawnRow(s);
    resolve(s);
    s.shotsSinceSpawn = 0;
  }
  s.shots++;

  for (let c = 0; c < s.cols; c++) {
    if (s.board[c].length > s.rows) { s.dead = true; s.events.push({ t: 'death' }); break; }
  }

  if (!s.dead) {
    s.ammo = s.queue.shift();
    s.queue.push(genAmmo(s));
  }
  return s;
}
```

Update the `const Core` object (add `shoot`):

```javascript
const Core = { createGame, genAmmo, smallestTile, gravityUp, findComponents, resolve, spawnRow, shoot,
  PREVIEW, AMMO_WINDOW, SPAWN_EVERY, TILE_MIN };
```

- [ ] **Step 4: 跑测试看它通过**

Run: `node games/abyssshoot/tests/test-core.js`
Expected: PASS — 到 shoot OK

- [ ] **Step 5: 提交**

```bash
git add games/abyssshoot/js/core.js games/abyssshoot/tests/test-core.js
git commit -m "feat(abyssshoot): shoot 交互入口(射击→结算→刷行→失败→换弹)"
```

---

## Task 8: 弹药放大区间断言（防死墙的核心平衡不变量）

**Files:**
- Modify: `games/abyssshoot/tests/test-core.js`

验 `genAmmo` 恒在 `[最小档 .. 最小档×4]` 内、且总是 2 的幂——这是防「大鱼变死墙」的硬不变量。

- [ ] **Step 1: 追加失败测试**（若已满足则直接通过，属回归保护）

Append to `games/abyssshoot/tests/test-core.js`:

```javascript
// --- 弹药放大不变量:恒在 [最小档, 最小档×4] 且为 2 的幂 ---
s = Core.createGame({ seed: 5 });
s.board = [[64, 64], [128], [256], [], []];   // 最小档=64
const lo = Core.smallestTile(s);              // 64
const hi = lo * Math.pow(2, Core.AMMO_WINDOW - 1);  // 64×4=256
for (let k = 0; k < 500; k++) {
  const a = Core.genAmmo(s);
  assert(Number.isInteger(Math.log2(a)), 'ammo 是 2 的幂');
  assert(a >= lo && a <= hi, `ammo ${a} 落在 [${lo}, ${hi}]`);
}
console.log('test-core: ammo 区间不变量 OK');
```

- [ ] **Step 2: 跑测试**

Run: `node games/abyssshoot/tests/test-core.js`
Expected: PASS（Task 2 的 genAmmo 已满足；本步锁死回归）

- [ ] **Step 3: 提交**

```bash
git add games/abyssshoot/tests/test-core.js
git commit -m "test(abyssshoot): 弹药放大区间不变量(防死墙)"
```

---

## Task 9: 蒙特卡洛 / 冒烟 —— 不变量 + 局长分布 + resolve 不死循环

**Files:**
- Create: `games/abyssshoot/tests/test-sim.js`

跑 N 局，每发随机选列，直到 `dead` 或步数上限。每步后断言：不抛异常、盘上值全为 >0 的 2 的幂、每列高度（存活时）≤ rows、无 NaN。收集局长（shots）分布，断言中位数落在合理带（防秒死/永生退化）。resolve 死循环通过给 `resolve` 一个迭代硬上限并断言未触顶来防。

> 说明：无尽刷分**没有「可赢性」可验**（区别于 mines 蒙特卡洛），本测只验「无退化局 + 局长分布合理 + 结算收敛」。带阈值为 P1 起始假设，跑出真实分布后回填收紧。

- [ ] **Step 1: 给 resolve 加迭代硬上限（防死循环，本身也是不变量保护）**

In `games/abyssshoot/js/core.js`, modify `resolve` 的 `while (true)` 段为带上限：

```javascript
function resolve(s) {
  let chain = 0, gained = 0, merges = 0;
  const MAX_ITERS = 10000;
  while (chain < MAX_ITERS) {
    const comps = findComponents(s);
    if (!comps.length) break;
    chain++;
    for (const comp of comps) {
      const nv = comp.value * 2;
      for (const cell of comp.cells) s.board[cell.c][cell.i] = 0;
      s.board[comp.anchor.c][comp.anchor.i] = nv;
      gained += nv * chain;
      merges++;
      if (nv > s.maxTile) { s.maxTile = nv; s.events.push({ t: 'newMaxFish', v: nv }); }
      s.events.push({ t: 'merge', v: nv, chain });
    }
    gravityUp(s);
  }
  if (chain >= MAX_ITERS) throw new Error('resolve 未收敛(可能死循环)');
  if (chain > 1) s.events.push({ t: 'chain', n: chain });
  s.score += gained;
  return { chain, gained, merges };
}
```

- [ ] **Step 2: 写 sim（会失败：文件不存在）**

Create `games/abyssshoot/tests/test-sim.js`:

```javascript
const assert = require('assert');
const Core = require('../js/core.js');

const GAMES = 300;
const STEP_CAP = 20000;   // 单局步数硬上限(防"永生"退化)
const isPow2 = v => Number.isInteger(Math.log2(v));

const lengths = [];
for (let g = 0; g < GAMES; g++) {
  const s = Core.createGame({ seed: 1000 + g });
  let steps = 0;
  while (!s.dead && steps < STEP_CAP) {
    const col = Math.floor(s.rand() * s.cols);   // 随机选列(用局内 rand,保持可复现)
    Core.shoot(s, col);
    steps++;
    // 逐步不变量
    for (let c = 0; c < s.cols; c++) {
      for (const v of s.board[c]) {
        assert(v > 0 && isPow2(v), `盘上值必须是 >0 的 2 的幂,得到 ${v} (seed ${1000 + g})`);
      }
      if (!s.dead) assert(s.board[c].length <= s.rows, `存活时列高 ≤ rows (seed ${1000 + g})`);
    }
    assert(Number.isFinite(s.score) && s.score >= 0, 'score 有限非负');
    assert(Number.isFinite(s.maxTile), 'maxTile 有限');
  }
  assert(s.dead, `局应在 ${STEP_CAP} 步内结束(seed ${1000 + g}) —— 疑似永生退化`);
  lengths.push(steps);
}

lengths.sort((a, b) => a - b);
const median = lengths[Math.floor(lengths.length / 2)];
const min = lengths[0], max = lengths[lengths.length - 1];
const mean = Math.round(lengths.reduce((a, b) => a + b, 0) / lengths.length);
console.log(`test-sim: ${GAMES} 局 | 局长 min=${min} 中位=${median} 均=${mean} max=${max}`);

// 防秒死退化:随机瞎打也不该中位数 < 8 步就死
assert(median >= 8, `中位局长 ${median} 太短,疑似秒死退化(需调 SPAWN_EVERY/AMMO_WINDOW)`);
console.log('test-sim OK');
```

- [ ] **Step 3: 跑 sim**

Run: `node games/abyssshoot/tests/test-sim.js`
Expected: PASS，打印类似 `test-sim: 300 局 | 局长 min=.. 中位=.. 均=.. max=..` 与 `test-sim OK`。
> 若 `median < 8` 断言失败：说明起始参数偏难,调 `core.js` 的 `SPAWN_EVERY`（调大=更温柔）或 `AMMO_WINDOW`，重跑，直到随机瞎打的中位局长进入合理带（这就是 P1 的蒙特卡洛调平衡动作）。把最终参数与观测到的分布记进本计划末尾「调平衡记录」。

- [ ] **Step 4: 提交**

```bash
git add games/abyssshoot/js/core.js games/abyssshoot/tests/test-sim.js
git commit -m "test(abyssshoot): 蒙特卡洛冒烟(不变量+局长分布+resolve 收敛上限)"
```

---

## Task 10: 挂进 root 测试脚本

**Files:**
- Modify: `package.json`（root）

> ⚠ 本仓多会话共用，改根级文件前先 `git status` 看别的会话有没有未提交改动；只 `git add package.json`，禁止 `git add -A`。

- [ ] **Step 1: 看现状**

Run: `git status --short && node -e "console.log(require('./package.json').scripts)"`
Expected: 看到现有 `test` / `test:mines` / `test:snake`；工作区除本计划产物外无他会话未提交文件。

- [ ] **Step 2: 加 test:abyss 并挂进 test**

In root `package.json`, 在 `scripts` 里加一行（放在 `test:snake` 之后）：

```json
    "test:abyss": "node games/abyssshoot/tests/test-tiles.js && node games/abyssshoot/tests/test-core.js && node games/abyssshoot/tests/test-sim.js",
```

并把顶层 `test` 改为串上 abyss：

```json
    "test": "npm run test:mines && npm run test:snake && npm run test:abyss",
```

- [ ] **Step 3: 跑单游戏测试**

Run: `npm run test:abyss`
Expected: 三个文件全 PASS（`test-tiles OK` / `test-core: ... OK` 多行 / `test-sim OK`）。

- [ ] **Step 4: 跑全量回归（确认没碰坏别的游戏）**

Run: `npm test`
Expected: mines + snake + abyss 全绿。

- [ ] **Step 5: 提交**

```bash
git add package.json
git commit -m "test(abyssshoot): 挂 test:abyss 进全量 test"
```

---

## 调平衡记录（Task 9 跑出后回填）

- 起始参数：`SPAWN_EVERY=6`、`AMMO_WINDOW=3`、`cols=5`、`rows=9`。
- 随机瞎打局长分布（min/中位/均/max）：__________（跑完填）。
- 调整：__________（若调了 SPAWN_EVERY/AMMO_WINDOW 记原因与新值）。
- 结论：核心手感规则确定、可跑、可调 → 进 P1b（render/main/index.html 可玩壳）。

---

## Self-Review（写完自查）

- **Spec 覆盖**（DESIGN §1/§8 的 P1 项）：5 列棋盘✓(T2)、射击入口✓(T7)、连通块合并结算✓(T4/T5 语义 a+b)、弹药放大✓(T2/T8)、向上重力✓(T3)、温柔刷行✓(T6)、唯一失败✓(T7)、连锁倍率✓(T5)、一套鱼梯✓(T1)、node 单测✓(T1-T8)、蒙特卡洛✓(T9)、无头冒烟✓(T9)、挂全量测试✓(T10)。render/main/DOM 明确留到 P1b，非本计划缺口。
- **占位扫描**：无 TBD/TODO；每步含真实代码与确切命令/预期。
- **类型/命名一致**：`s.board`(数组的数组,index0=顶)、`gravityUp`/`findComponents`/`resolve`/`spawnRow`/`shoot`/`genAmmo`/`smallestTile` 全程同名同签名；常量 `PREVIEW/AMMO_WINDOW/SPAWN_EVERY/TILE_MIN` 一致；事件 `t` 值 `shoot/merge/chain/newMaxFish/spawn/death` 一致；锚点 `{c,i}` 结构一致。
- **确定性**：随机全走 `s.rand`（禁 `Date.now`）；resolve 单趟+固定扫描序+固定锚点+迭代上限 → 可复现、可测、防死循环。
