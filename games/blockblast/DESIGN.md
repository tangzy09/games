# blockblast —— 设计文档 v4（终稿：调研 + 模拟数据 + 红队三轮之后）

> **状态（2026-07-13）：P1-P4 全部完成，已提交 App Store 审核**（ASC「Cube Blast: Block Puzzle」，Apple ID 6790598746）。
> 线上 web：<https://blocks.ai-speeds.com> · 商店页实际值见 `docs/store-listing.md` · 工程约定见 `CLAUDE.md`。
> **1.0.1（2026-07-30/31，web 已上线，iOS 待出包）**：①变现批——无尽结算金币 + 看广告×2、金币皮肤、结算统计、种子挑战（§2 承诺 4）；②体验批——死亡序列/濒死心跳/宽限可视化/非法虚线/重震动/「继续而非重开」/FTUE 指引/设置页等（§2/§8 欠账全还）；③内容批——**30 关 3 章 + 章末宝箱 + 拼块自带水晶（§6 另一半）+ 每日日历补玩 + 水晶图鉴**；④广告模型定稿（§9.1：前 50 盘零插屏/每 10 盘至多 1 个/只在赢时；IAP 封存）；⑤收集批——**天使画廊 500 张 + 16 皮肤（10 套盘数白送）**；⑥留存批——**每日任务 + 连续奖励阶梯 3/7/14/30 + 金币补签 + 统计页 + 每日分享 + 目标提示条 + 推送/求好评/反馈（原生）**；⑦**天使榜**（§7 降级方案落地）。

> **1.0.2（2026-08-01/02，web 已上线，iOS 待出包）**：
> ①**🔍 运行时教练**（新增 §6.5）—— 把验关卡的求解器搬进游戏：提示 / 死亡复盘 / 妙手 / 「我的弱点」页；
> ②**激励视频 4 位 → 8 位 + 每日额度**（§9.2 新表；补掉「看广告领币零 cap 可无限刷」的洞）；
> ③**金币出口扩容**（§9 表：+4 款高价皮肤，金币也能买礼包/图鉴/方块）；
> ④**等级/称号 XP 条**（抽进 `engine/meta.js`）；⑤**天使画像去重 −26MB**（走 `engine/angels.js`）；
> ⑥**5 种水晶形状双编码**（无障碍：一色一形）；⑦**音效重做**（§8.2：6 → 12 个音 + 离线渲染验收）；
> ⑧**主界面天国装饰 + 关卡提成主按钮**；⑨**30 关 → 300 关 / 30 章**（§6.3 的曲线改成程序化生成 + 自动收敛门禁）；
> ⑩**🧱 看广告送方块**（§2 末尾专门解释：它**不动块流**，公平承诺不受影响）。

> **内部代号 `blockblast`（目录名），⛔ 绝不能当商店名** —— *Block Blast!* 是 Hungry Studio 的商标+爆款。商店名二期另取原创名。
> **定位**：8×8 block puzzle，关卡模式优先。root `CLAUDE.md` + `engine/README.md` 先读，本文件只讲本游戏专属设计。
>
> **v4 是可以直接开工的版本。** v1 拍脑袋 → v2 补规格 → v3 上网调研 → **v4 = 跑模拟 + 红队自我攻击后的定稿**。
> 数值不是猜的：`tools/sim.js`（本目录）跑了 8×8 全规则 × 三档 AI × 三种发牌策略 × 数千局，**下面每个数字都可复现**：`node games/blockblast/tools/sim.js 500`。
> **v4 砍掉了 v3 的两个核心机制（DDA + 保底发牌）和一个招牌（perfect clear 当主打）** —— 理由全在 §0，先读它。

---

## 0. 六个已验证的结论（v4 的全部依据）

### 0.1 计分公式的量级是对的 ✅（模拟验证）

参考 AI（mid 档）中位分 **1698**，参考截图里那局是 **1703**。中位局长 **98 次落子 ≈ 4 分钟**，正中「单局 3-5 分钟」目标。**§3 的公式保留**（只改 streak 封顶，见 0.4）。

**P1 实测复核**（sim 直接驱动真 `js/core.js`，600 局，已修正 game-over 判定）：中位分 **2971**、中位落子 **143**、**61.2% 的局能摸到 7 连满档 ×4**。
单局落子数偏向上限（判读线 >150 算跑偏）—— 若实机觉得一局拖太久，旋钮是**盘面拥挤度**，不是计分公式。

### 0.2 ❌ 砍掉「保证至少一块可放」——它是个假机制（模拟证伪）

| 发牌策略 | 中位分 | 中位局长 | 触发重抽次数/局 |
|---|---|---|---|
| 纯随机（原版行为） | 1698 | 98 | — |
| **保证至少一块可放**（v3 的硬保证） | **1720** | 94 | **0.04** |

**每局只触发 0.04 次，玩家永远感受不到。** 原因：死亡几乎从不是「三块全放不下」，而是「三块里**有一块**放不下」——因为**三块必须全部放完才补牌**。v3 的保证约束了一个几乎不发生的事件。
它却要付出两个代价：与「下一组预览」数学互斥（预览时校验的棋盘 ≠ 发牌时的棋盘）、且让「公平宣言」变成可被证伪的谎言。**为一个 0.04 次/局的机制赔上两个差异化 —— 砍。**
（对照：真正有效的是「保证三块全放完」，但它把局长从 98 拉到 338、分数拉到 6705 —— 游戏基本不会死，废掉整个品类的张力。也砍。）

### 0.3 ❌ 砍掉 DDA——它对手感的影响测不出来，却毁掉核心差异化（模拟证伪 + 红队）

| DDA | 中位分 | 中位局长 | PC 局占比 |
|---|---|---|---|
| **off**（固定权重，完全不看棋盘） | 1695 | 98 | 2.0% |
| on（v3 那套按 fill 调权重） | 1721 | 92 | 2.4% |

**差异 1.5%，测不出来。** 而红队一针见血：`fill<0.30 → 多发大块` 翻译成玩家语言就是「**我盘面干净的时候它专门发大块卡我**」——这正是「rigged」指控的原型。玩家从来没指控游戏读分数，他们指控的就是「**游戏在看我的处境**」。而 DDA 就是在看。
更讽刺的是 v3 那条「连续 3 回合无消除 → 发直条」**就是一个 pity timer**，而我在调研里刚把「声称有 pity timer」的站点判为编造垃圾；它还违反了 v3 自己写的「dealer 无历史记忆」（自己的单测会把自己判死）。

### 0.4 ✅ 由此得出本作真正的招牌：**预生成块流（Piece Stream）**

砍掉 DDA + 保底之后，dealer 塌缩成一件极简、且**极强**的事：

> **一局的整个出块序列，在你落第一子之前就由种子完全确定了。它不看棋盘、不看分数、不看你付没付钱——它根本不知道你在做什么。**

这一句同时解决了四个问题：

| 它解决了 | 怎么解决的 |
|---|---|
| **公平（品类第一痛点）** | 这是**可验证的强承诺**，不是公关辞令：游戏内显示本局种子，玩家可复现整条块流。头部产品没一个敢这么做。ASO 主打语：**"The pieces are decided before you start. We can't see your board."** |
| **下一组预览** | 块流是预生成的 ⇒ 预览天然成立，**不存在 v3 那个"预览过的牌被偷偷换掉"的矛盾** |
| **每日谜题** | 同一种子 = 全球同一条块流 = 真正同一道题，**分数可比、可做榜**（v3 那版依赖棋盘的 dealer 做不到这点，红队 S8 指出） |
| **可测试性** | dealer 变成纯函数 `stream(seed, i)`，单测/复现/撤销回滚全部变简单 |
| **种子挑战（1.0.1）** | 结算页「挑战好友」分享 `?seed=` 链接 ⇒ 对方拿到**逐块相同**的块流，同题比分。头部克隆一家都做不了（它们的发牌看棋盘）。挑战局不写无尽存档、不进无尽最高分（同种子可反复练，混进榜就不公平），重开 = 同种子再练 |

**失败因此变得可归因**：块流是命定的、你还能看见下一组——输了就是**你摆错了**，不是它坑你。（调研原话：转投 Woodoku 的玩家说那边公平，正因为"输了觉得是我菜"。）

### 0.5 ⚠ perfect clear 撑不起招牌，但「近清盘」可以（模拟数据）

PC 每 50 局才出一次 —— 按「每天 2.7 局」算，玩家**半个月见一次**，做招牌是自欺欺人（红队 F6 判对）。
但「差一点点清空」每几局就有一次 —— 招牌要建在这个梯度上。见 §4 的 **SWEEP**。

**⚠ P1 实测修正（`tools/sim.js` 已改为直接驱动真 `js/core.js`，数字比 v4 初稿更准）**：

| 事件（mid 参考 AI，1200 局） | 实测 | 说明 |
|---|---|---|
| **SWEEP 局占比**（一局里至少触发一次） | **18.8%**（约每 5 局一次） | 招牌是活的 |
| **PERFECT 局占比** | **0.0%**（1200 局零次） | 见下 |

**PERFECT 掉到 0 不是 bug，是加了 `SWEEP_FLOOR=16` 之后的必然**：v4 初稿报的那 2%，**几乎全是「开局空盘顺手清掉 8 格」的白嫖**（单测抓到：8×30+300 = 540 分 ≈ 中位一局总分的 1/3，可稳定复现的刷分套路）。堵掉白嫖之后，**真正的清空（落子前盘面 ≥16 格）本来就极其稀有**。

⇒ **PERFECT 的正确定位 = 技巧天花板，不是日常爽点**：参考 AI 不追求它 ⇒ 一次也碰不到；而人类**刻意去凑**（留出交汇的行列）是可以做到的 —— `tests/test-core.js` 用构造盘面证明了它可达（行0+行1+列7 三条同时满 → 一步清空），**它是稀有，不是死代码**。这正是纯克隆最缺的东西：一个只有想拿的人才拿得到的东西。

### 0.6 ⚠ 新手的问题，发牌救不了 —— 只有教学能救（模拟数据，反直觉）

| 拼块权重（小/中/大） | casual 中位局长 | mid 中位局长 |
|---|---|---|
| 25/55/20（基准） | 15 | 98 |
| 45/45/10（大幅偏小块） | 19 | **419** |
| 55/37/8（极端偏小块） | 21 | **1052** |

**把小块权重翻倍，新手只多活 4 步；熟手的局长却爆炸到 10 倍。** 新手死得快不是因为块大，是**不会摆**。
更扎心：**casual 玩家的最长 streak 中位只有 2** —— 新手**根本体验不到 streak/combo 这个核心爽点**。
⇒ **权重锁死 25/55/20**（熟手 98 回合 ≈ 4 分钟，正确）；**新手全部交给关卡模式的前 5 关**：用预置盘面手把手喂他一次连击（§6.3）。这条是 FTUE 的生死线。

### 0.6b ⚡ 快速放置奖励（2026-08-04）

连续在 **2.5 秒内**落子 ⇒ 链 +1，给 `5 × min(链, 5)` 分（+5/+10/…/+25 封顶）；慢一手**只是归零**。

| 约束 | 为什么 |
|---|---|
| ⛔ 每日谜题 / 种子挑战**不给** | 那两条赛道承诺「同种子分数可比」（§2）——掺进手速就不是同一道题了。与礼包手同一个先例 |
| ⚠ 时间戳**从外部传入**（`place(s, slot, r, c, now)`） | core 保持纯函数；**不传 = 不给** ⇒ 模拟器 / 参考 AI / verify-levels / 老测试的行为一个字节没变（sim 中位分仍落在 2971 基线上） |
| ⚠ 撤销要回滚 `lastPlaceAt`/`speedChain` | 否则「撤销→重放」可无限刷分 |

量级：一局 ~140 手，理论上限约 +3500，实际远低于此（要一直保持 2.5s 内落子）。相对中位分 ~2971
是**显著但不主导**的一层，不会把 streak 从「主引擎」的位置上挤下去（§3 的基调不变）。

### 0.7 两条不动的基调

- ⛔ **永不加倒计时/下落压力**。玩家把它当睡前放松游戏，相对 Tetris 的核心优势就是「块在托盘里等你」。
  - ⚠ **这条没有被 2026-08-04 的「快速放置奖励」推翻 —— 两者的区别要说清楚**：
    **倒计时 = 惩罚慢**（不快就死/扣分）⇒ 仍然永久禁止；
    **速度奖励 = 奖励快**（不快也一分不少）⇒ 纯增益，「块在托盘里等你」的承诺不变。
    落实成三条硬约束（`core.js` 有测试钉死）：**慢了不扣分、不断 streak、UI 上绝不画倒计时条**。
    呈现也必须是**事后惊喜**（落子后弹一下 `⚡ +N`），⛔ 不许出现任何「还剩几秒」的暗示 ——
    那本身就是压力源，即使不带惩罚。
- 📉 真实数据（第三方）：D1 ≈ 26%、单次会话 ≈ 12 分钟、每天 2.7 次 ⇒ **单局 3-5 分钟，一次会话连打几局**。靠体量不靠深度，别做重系统。

---

## 1. 核心规则

**⚠ 全文术语（先定义，否则整套公式没有单位——红队 F1）：**
- **回合（turn） = 一次落子（place）**。所有 streak / 计分 / 计数器**一律以落子为单位**。
- **一手（hand） = 托盘里的 3 块**。**三块必须全部放完**才补下一手。
- **游戏结束**：当前手里**剩余的每一块都无处可放** ⇒ game over。
  ⚠ **绝不是「任一块放不下就结束」**（P1 实机被一眼看穿的错，已修）：剩 2 块、一块能放一块不能时，玩家有权**先放能放的那块** —— 那一步可能**消掉一行、腾出空间**，卡住的块就又能放了。只有所有剩余块都塞不进去才算死。
  放不下的块在托盘里**变灰**（失败要看得见原因）。

| 项 | 规则 |
|---|---|
| 棋盘 | **8×8** |
| 托盘 | 3 槽，一手 3 块，**全放完才补** |
| 旋转 | **不可** |
| 消除 | 填满的整行/整列全清；一次落子可同时消多条 |
| 计时 | **无** |
| **下一组预览** | **常驻显示下一手的 3 块**（§0.4；可在设置关掉 = 硬核模式） |

### 1.1 拼块表（32 种，逐块列权重 —— 不可旋转 ⇒ 每个朝向是独立块）

> ⚠ 红队 S2：v3 说「19 种」但列表展开是 32 种，且只给了档权重、没给块权重 ⇒ 权重表不可实现。这里逐块给死，`pieces.js` 直接照抄。

| 档（合计权重） | 块（各自权重） |
|---|---|
| **小 = 25**（9 种） | `1×1`=4 · `1×2`=4 · `2×1`=4 · `1×3`=3 · `3×1`=3 · 小L ×4 向 = 各 1.75 |
| **中 = 55**（17 种） | `2×2`=6 · `1×4`=5 · `4×1`=5 · L/J ×8 向 = 各 2.5 · `S`/`Z` ×2 向 = 各 2.5 · `T` ×4 向 = 各 2.25 |
| **大 = 20**（6 种） | `1×5`=4 · `5×1`=4 · 十字=3 · `2×3`=3 · `3×2`=3 · **`3×3`=3** |

- 全部走引擎 `PRNG`（mulberry32），**禁 `Math.random`**。**无 DDA、无重抽**（§0.2/0.3）⇒ **玩家实际经历的分布 = 上表声明的分布**（v3 有 rejection sampling 会扭曲声明权重，红队 S2 指出；现在不会了）。
- **单测**：10 万次抽样的实际分布 ≈ 声明分布 ±1%。
- 颜色纯装饰（消除只看行列填满）。

### 1.2 块流（Piece Stream）—— dealer 的全部实现

```js
// dealer.js 的全部公开面（纯函数，无状态，不看棋盘）
stream(seed, i) -> piece      // 第 i 块是什么，完全由 (seed, i) 决定
```
一局开始定 `seed`（随机 / 每日谜题用日期 / 分享用给定种子）。`G` 只存 `seed` + `streamIndex`。
⇒ 存档、撤销、复现、每日谜题、E2E 固定用例，全部免费得到。

---

## 2. 公平：三条可验证的承诺（写进游戏内「公平」页 + 商店描述）

> 这是本作**唯一的、也是最强的**差异化。品类第一痛点是「rigged」感（调研：全品类行话，无人敢正面回应）。我们不辩解，直接把发牌变成**可被玩家验证的**。

1. **出块序列在你落第一子前就定好了**，由本局种子决定 —— **游戏内可查看/复制种子**，用同一种子重开，块流逐块一致。
2. **发牌不读取任何玩家状态**：不看棋盘、不看分数、不看是否接近纪录、不看是否付过费。
   → **可测化**（单测）：`stream()` 的签名里根本没有棋盘/分数参数；再加一条断言：把任意棋盘/分数/付费状态注入全局后，同 seed 的块流逐字节不变。
3. **权重表公开**（§1.1 原样印在公平页里）。无 pity timer、无难度自适应、无「记忆你上一步需要什么」（调研里玩家最恨的那条：「下一块永远是我上一步需要的」）。

**并且失败必须可归因**（红队 F4/调研共识）：game over 时，把当前手的每一块**逐一在棋盘上红色扫一遍**，演示"确实每格都放不下"，而不是一句干巴巴的 "No moves"。
**1.0.2 又往前走了一步**：死亡序列证明完「都放不下」之后，结算卡再回答**为什么会走到这一步** ——
「第 37 手换个位置放，还能再走 12 步」（§6.5 的死亡复盘）。⛔ 口径仍是**陈述事实**，不是指责。

### ⛔ 2.1 三个「看起来像在改发牌」的东西，其实都没碰块流

承诺 1/2 是这个产品的地基，任何新机制都要先过这一关。目前有三个容易被误会的：

| 机制 | 它做了什么 | 为什么不破坏承诺 |
|---|---|---|
| **换一手（刷新）** | `streamIndex += 3` | 只是**跳过**这一手，跳到的正是你早就能预览的下一手 —— 改变不了后面是什么 |
| **🧱 送方块（1.0.2）** | 接下来 2 手托盘全是 1×1 | **`streamIndex` 一动不动**：礼包手期间**不从块流取**，用完之后从**原来那一块**继续，一块不差、一块不换 |
| **🔍 教练（1.0.2）** | 提示 / 复盘 / 妙手 | **只读**：它只看当前盘面 + 公开的块流，不写任何状态、不碰随机源 |

三条都写成了断言（`test-core` 的「礼包手不动块流」、`test-coach` 的「疯狂使唤教练之后块流逐块不变」、
`e2e-rewards` 的 `streamIndex === before`）。**新增任何影响托盘的机制，先在这张表里给它一行。**

---

## 3. 计分（v3 公式保留，只改 streak 封顶 —— 模拟校准）

```
落子分 = 拼块格子数

消除分 = 20 × L × comboTier(L) × streakMult
  L = 本次落子同时消掉的行+列总条数
  comboTier: L=1 → 1.0 | L=2 → 1.5 | L=3 → 2.2 | L≥4 → 3.0
  streakMult = 1 + 0.5 × min(streak - 1, 6)      // 上限 ×4，7 连即封顶
  streak = 连续「有消除」的落子数

streak 断裂 = 连续 2 次落子零消除     // 给手气差一步的容错，直接对冲「被坑」感
  （宽限中的那 1 次零消除：streak 值保持不变、不增长）
```

**为什么封顶从 13 连改成 7 连**：模拟显示参考 AI 的**最长 streak 中位数就是 7**，而 v3 要 13 连才封顶 —— **只有 1.5% 的局摸得到**，等于设计了一个几乎没人见过的天花板。现在中位玩家在一局里**刚好能摸到一次满档**，天花板才有意义。

**为什么 streak 是主引擎而不是「一次多消」**：调研结论——玩家的爽感来自「**每一步都有回报**」，攻略圈共识是「连续 5 步各消 1 条 ≫ 一步消 5 条再空放 4 步」。comboTier 只是锦上添花，不是主菜。

---

## 4. SWEEP / PERFECT：招牌大招（数据重定的，替换 v3 那个死招牌）

> §0.5：清空整盘只有 2% 的局能碰到（做招牌 = 自欺欺人），但**"剩 ≤4 格"有 33.8% 的局能碰到**。招牌建在梯度上。

**触发**：一次落子消除后，看棋盘剩余占用格数 `left`：

| 事件 | 条件 | 奖励 | 出现频率（mid，实测） |
|---|---|---|---|
| **SWEEP** | `left ≤ 8` | 分数 `+ cleared × 8`，streak `+1`，屏幕金色横扫特效 + `SWEEP!` | **约每 4 局一次**（实测 23.7%）|
| **DEEP SWEEP** | `left ≤ 4` | 分数 `+ cleared × 15`，streak `+2`，更强特效 | 含在上面的 23.7% 内 |
| **PERFECT CLEAR** | `left == 0` | 分数 `+ cleared × 30 + 300`，streak `+3`，**全屏白闪 + 金光爆开 + `PERFECT!` + 最高音效 + 长震动** | **参考 AI 1200 局零次** ⇒ **技巧天花板**：只有刻意去凑的人才拿得到（单测已构造证明可达，见 §0.5）|

`cleared` = **本次落子前的已占用格数**（红队 F6：这样"清一个 30 格的盘"是大爆炸，"开局空盘顺手清掉 8 格"≈ 没奖励 —— **杀掉了 v3 那个"开局白嫖 PERFECT"的可刷套路**）。

**⚠ 与「每步都消」的主引擎不冲突**（红队 F6 的最后一击）：奖励与 `cleared` 挂钩 ⇒ **想要大 SWEEP，就得先让盘面涨起来、再一口气清掉**，而涨盘面的过程中你依然在**每步消除**保 streak。它是主引擎的**放大器**，不是"憋大招不消除"。
**关卡模式里** `left` **只算可消除格**（石块不计），否则含石块的关永远触发不了（红队 F6）。

---

## 5. 拖拽（⚠ 引擎缺口，本作第一个真拖拽游戏）

`engine/input.js` 只有 tap/swipe。drag 层写在 `games/blockblast/js/drag.js`，**不动 engine**（改 engine 牵动 4 个线上游戏，root CLAUDE.md 明写的坑）。第二个拖拽游戏出现时再抽取。

| 参数 | 定值 |
|---|---|
| **锚点** | **手指按下时所压住的那一格**；若按在 bounding box 的空洞上，取最近的实心格。（红队 S5：L/S/T 的"中心"可能不在块上，v3 没定义 —— 这是本作最核心的一个数字） |
| 抬起偏移 | 拼块上移 **min(1.2 格, 到棋盘顶部的可用空间)**，并避开安全区/刘海（小屏抓顶行会把块推出屏幕） |
| 拾起放大 | 1.0 → 1.1×，80ms |
| 幽灵预览 | 合法：目标格半透明 + **将被消掉的整行/整列整条发光**；非法：拼块变暗 + **描边虚线**（不靠红/绿区分 —— 色盲） |
| 回弹 | 非法松手 → 180ms ease-out 弹回 |
| **与 engine Input 的冲突** | 拖拽期间 `G.phase='dragging'`；**棋盘/托盘区域不注册 `addHit()`** ⇒ engine 的 tap 无区域可命中，放下拼块的那次 pointerup 不会被 hitTest 误判成一次点击（红队 S5） |

---

## 6. 模式 A：关卡收集（P2，主线）

- **目标条**：1-3 种水晶 + 数量，达成打勾。全达成 → 胜利。
- **水晶**：① 关卡预置；② 托盘拼块自带。**只有该格被消行/消列清除时才计入**（教程原话「Burn lines and collect crystals」）。⇒ 关卡的乐趣 = "必须打通**那一条**线"，竞速是"消哪条都行"。
- **石块**：不可消除的惰性格（含石块的行/列永远消不掉），关卡预置。

### 6.1 ⛔ 软锁死：必须堵的死（红队 F4，会毁掉整个关卡模式）

石块永不消 + 水晶只在消除时计入 + 玩家又死不了 ⇒ **一颗水晶若落在「行有石块 ∧ 列有石块」的格子上，永远收集不到，而玩家永远死不了 —— 既不 win 也不 lose，存档还会把这个死局存下来。**

三道防线，缺一不可：
1. **关卡数据校验**（构建期）：石块所在行/列的格子**禁止承载水晶**；托盘自带水晶的块**禁止落在这类格子**（运行时也禁止）。
2. **不可胜检测**（运行时）：任一目标水晶已不可能被清除 ⇒ **立刻判负 + 免费重开**。
3. **`tools/verify-levels.js`**（P2 的一等公民，不是可选项）：每关跑 200 次参考 AI × 不同种子，**通关率 < 80% 的关卡不许进包**。没有这个工具，30 关就是 30 个"看运气"的地雷。

### 6.2 ⛔ 关卡失败**不出广告**（红队 F4 的第二击）

v3 写"失败 → 看广告换一批托盘继续"。**这就是我自己在 §9 痛骂的东西，只是换了我的名字**：一个因运气打不过的关，出口是广告。
**定死**：关卡失败 = **只给「立刻重来」，零广告、零插屏、零续命兜售**。关卡的广告位只放在**通关结算**（正反馈时刻）。

### 6.3 难度曲线（前 5 关是 FTUE 生死线 —— §0.6）

| 关段 | 设计 | 意图 |
|---|---|---|
| **1-2** | 棋盘预置成"**一放就消**"（某行已填 6 格，托盘正好给 2 格块） | **保证新手 30 秒内亲手消掉第一行**（数据：casual 玩家在无尽模式里最长 streak 只有 2，**根本碰不到 combo**） |
| **3-5** | 预置成能连续消 2-3 次 | **手把手喂一次 streak** —— 不喂，新手永远不知道这游戏爽在哪 |
| 6-10 | 1 种水晶 → 2 种，教"要规划先打哪条" |
| 11-18 | 预置阻塞块 + 石块，教"空间要省" |
| 19-30 | 3 种水晶 + 角落/边列水晶（边列最难填满） |
| **31-300**（1.0.2） | **程序化生成**：`autoSpec(id)` 按难度 d 铺参数，章内由易到难走一个小波、章间缓慢加压 | 手写 300 关不可能；曲线**只在已知安全区里走** |

**300 关 / 30 章的生成与门禁（1.0.2）**：

- 难度旋钮仍是那三个（线数 × 每条线的空格 × 石块数），d ∈ [0,1] 映射到**安全区**：
  线数 **≤4**（§6 早就测出 4-5 线时 AI 通关率崩到 60%）· 石块数与线数**联动封顶**（2 石块封 4 线 = −20%）·
  `pieceCrystals.goal` **只取 2-3**（首版 goal 5 把关打到 46%，被 verify 拦下）。
- 流程一条命令：**`npm run fix:levels`** = 生成 → 跑门禁 → **只把 <80% 的关降一档重生成** → 再跑，
  收敛后**自动跑 `--write` 标定 par**。实测 3 轮收敛，300 关全部 ≥80%（200 seeds）。
- ⛔ **两个必须记住的坑**：
  ① 生成器产出的关**没有 `par` 字段**，而 `starsFor()` 在 `!par` 时直接返回 1 星 ⇒
  忘了标定 = **300 关的三星系统整个失效、星星经济归零**，而且没有任何测试会报错；
  ② 修复循环**别拿显示的百分比当判据** —— `toFixed(0)` 会把 79.5% 显示成 80%，
  循环以为过了、verify 自己判不过，两边打架（实锤：卡在第 298 关）。只认 verify 打的 ✗ 标记。

### 6.4 三星（重定 —— v3 的阈值让三星白送）

> 红队 S1：v3 的 `剩余空格 ≥24 → 三星` ⇔ `fill ≤ 62.5%`，而 §0 自己把 `fill≥60%` 定义为"濒死区" ⇒ **只要不是快死的时候过关就是三星**；而且关卡不限步，玩家可以先把盘面清干净再收官 —— 三星**没有代价**，星星经济当场归零。

**改为按落子数评**（`N` = `verify-levels` 跑出的参考 AI 通关中位步数）：
`≤ N` → ⭐⭐⭐ ｜ `≤ 1.4N` → ⭐⭐ ｜ 否则 ⭐。**用过撤销/刷新 → 最高 ⭐⭐。**
⇒ 不限步（不会输）**但要三星就得省步** —— 张力回来了，重玩理由也回来了。

### 6.5 🔍 运行时教练（1.0.2 新增 · `js/coach.js`）

**它不是新玩法，是把已经写完的东西再卖一次。** §6.1 的门禁一直在跑一个参考 AI（算每关通关率），
但它只活在构建期，玩家一辈子见不到。把同一套东西搬进运行时（纯函数、可 node 单测），当场长出四样：

| 出口 | 内容 | 成本 |
|---|---|---|
| 💡 **提示** | **这一手的最优落点**（不是启发式挑一个「看起来不错的」） | 每局首次免费，之后看广告（3 次/天） |
| 🔍 **死亡复盘** | 「第 37 手换个位置放，还能再走 12 步」——对最近 3 手各跑 top-4 候选的**生存模拟** | 趁死亡动画播放期间异步跑（实测 ≈4ms） |
| ✨ **妙手** | 落子前判分：同消 2 条 / 收到水晶且没有更优 ⇒ toast | `judge` 每手都跑，实测 ≈0.1ms |
| 📉 **「我的弱点」页** | 两类**纯逻辑可判定**的失误累计：有能消的行没消 / 一手造出 ≥2 个孤格 | 静默记账，零额外埋点 |

**四条边界（都踩过，别退回去）**：
1. 判据必须**能自证**——只标纯逻辑可判定的失误，绝不标「你打得不好」；
2. ⛔ **顶尖手带失误标签也不判失误**：求解器自己都把它排进前 15% 时，那多半是有道理的取舍，只是不表扬；
3. **只在两头说话**（妙手 + 死亡复盘），局中失误一律静默 —— 每手都点评的教练是烦人精；
4. **算不出结论就闭嘴**（复盘的收益 < 3 步就不出那一行），编出来的诊断比没有更伤。

⚠ **运行时的评估函数故意不与 `tools/verify-levels.js` 共用**：那份是通关率/par 的地面真值，
改它 = 300 关全部要重标；这份偏「活得久」（无尽为主）。两者各自为政，谁也别悄悄污染谁。
⛔ 教练**只读、不碰随机源**（见 §2.1）。

---

> **✅ 2026-07-31 已按本节的「降级方案」落地为「天使榜」（js/ghosts.js）**：20 个预设分数的**明示虚构角色**
>（名字 Coco/Luna/Aurora…，头像用天使画廊图），200→20000 按 sim 数据铺档；局中超越即 toast、
> 结算页对比行、独立榜页、菜单目标条挂「差 ≤600 分」目标。**红线兑现：文案零「玩家」字样（单测钉死）**；
> 进度零存档（由最高分推导）。下面原文保留作设计依据。

## 7. 模式 B：幽灵追赶（P4，可砍 —— 已降级）

> 调研：**原版 Block Blast 并没有对手竞速模式**（截图疑为山寨/网页换皮）⇒ 未经市场验证。
> 红队 S10：v3 一边要「你已击败 N 位玩家」的社会证明，一边要求「bot 必须明显是虚构」—— **社会证明之所以有效，恰恰因为玩家以为对面是真人**。二者是同一件事，不能既要又要；「击败 N 位**玩家**」这句文案本身就是伪装。

**降级方案**：改成**追赶幽灵**——你自己的历史最好成绩 + 若干**明确标注的 AI 幽灵**（名字如 "AI Ghost · 1200"）。进度条与超越反馈保留（把无尽模式的空虚计分切成一串小胜利），但**文案绝不出现"玩家"**。
**优先级降到 P4，允许直接砍。** 若砍，§10 的元层用「每日谜题 + 成就 + 图鉴」补位。

---

## 8. Juice（优先级 = 核心逻辑；调研里玩家点名夸的全是这些）

| 时刻 | 反馈 |
|---|---|
| 落子 | 下压 + 落地弹（1.05→1.0, 100ms）+ "啪" + 轻震动 |
| 消行 | **逐格延迟扩散消失**（每格 20ms，自消除中心向两端）+ **发光→碎裂→彩虹粒子尾迹**。扩散感 = "我引爆了它" |
| 多条同消 | 屏幕轻震（随 L 递增）+ 升级式赞美词 `Good!`→`Great!`→`Amazing!`→`Unbelievable!` |
| streak | 音效**音高沿音阶上行**（C-D-E-F-G…）—— 最便宜也最上瘾的正反馈 + `COMBO ×3.5` |
| **SWEEP / PERFECT** | §4 |
| 收集水晶 | 水晶**飞向顶部目标条**（贝塞尔 300ms）+ 数字跳动 + "叮" |
| 危险 | `fill ≥ 0.75` 时分数区**心跳图标加速**（不给文字警告，只给生理紧张） |
| game over | §2 的"逐块红色扫盘"证明 + **一键立刻重开、零惩罚** |

### 8.1 ⚠ fx.js 是 P1 的独立工作项，不是"一个特效"（红队 S3）

引擎**没有 tween / 粒子 / 动画时间线**，全得在 `fx.js` 从零写；而引擎契约是**每帧全屏重画**，主力机型是**低端安卓 WebView**。
**性能预算（写死）**：粒子总数 **≤ 300**（超出丢弃）；fx 帧预算 **≤ 8ms**；**低端设备/设置项可整体关闭粒子**（只留位移+缩放）；**fx 必须能整体关闭且不影响 core 逻辑与测试**。
音效：**WebAudio 合成**（音阶上行、SWEEP/PERFECT 音、4 个赞美词音、落子/消行/水晶）—— 零素材成本，且音阶最好做。列进 `GAME_CONFIG.sfx`。

### 8.2 音效（1.0.2 重做：6 → 12 个音 + 离线渲染验收）

原来是 6 个单振荡器 blip，而且**收水晶/领奖/任务完成/榜上超越全在复用 `sweep('sweep')`** ⇒ 听感上"什么都一样"；
菜单/商店/图鉴整片界面**一个反馈音都没有**；解锁成就完全静音。现在：

- **总线**：master → 压缩器 → 输出，外加一条并行**混响**（IR 也是合成的噪声衰减，仍然零素材）。
  ⚠ **短促 UI 音必须走 `dry` 绕开混响**：0.9s 的尾巴挂在 0.05s 的点击上，连点糊成一片
  （工具量到 `place` 发声 0.49s → 改后 0.08s）。
- **响度**：原来峰值只有 0.03~0.29，玩家得把系统音量开很大 ⇒ 推 master + 压缩器兜底（标准响度链路）。
  现在 PERFECT ≈0.72、tap ≈0.12，阶梯可量。
- **12 个音**：place / clear（音阶上行 + 五度泛音）/ sweep(3 档) / over / pick / tap / invalid /
  **coin**（叮当，越多越密）/ **collect**（玻璃质感）/ **brilliant** / **levelUp** / **heartbeat**（濒死氛围）。
  每次播放**微随机**（±1.2% 音高 / ±6% 时长）—— 同一个音听两百遍只剩噪音感。
- ⭐ **`AudioContext` 可注入** ⇒ 同一份代码能跑在 `OfflineAudioContext` 上：
  `node tools/audit-sfx.cjs` 渲染 16 个 wav 到 `C:/tmp/blockblast/sfx/`（含顺序播全部的 `all.wav`），
  并断言**峰值不削顶 / 不是静音 / 拖尾不超时**。
  ⛔ 机器只能挡这三类，**好不好听必须真的听一遍** —— 音效是典型的「测试全绿也可能很难听」。

---

## 9. 变现（调研：品类第一杀手是广告，不是发块）

### ⛔ 三条红线

1. **绝不局中插屏**（Woodoku 重灾区：广告盖住棋盘导致误放块 —— 它直接偷走一局）。
2. **绝不在玩家拒绝复活广告后强塞无奖励广告**（Block Blast 被骂最狠的一条：拿走了玩家"我不看"的选择权）。
3. **失败时不推广告**：关卡失败只给「立刻重来」（§6.2）。插屏**只在正反馈时刻**（通关结算后），且**每 3 局最多一次**。
   （v3 写的"插屏 ≤15 秒可跳过"**做不到** —— AdMob 插屏的时长/可跳过性由广告主决定，SDK 没这个开关。红队 S6。只承诺我们真能控制的：**位置 + 频次**。）

### ✅ 必做

- ~~一次性「去除所有广告」IAP（$2.99）~~ → **2026-07-31 封存**（见 §9.1：插屏降到近乎象征性后，付费去广告的动机消失；ASC 资产保留可随时启用）。「买了之后功能不变少」的原则保留给 `wallet.noAds` 历史用户。
- 激励视频（全部玩家**主动**触发）：换一手 / 撤销 / 领金币 / 结算×2。每游戏自备 adUnits，**绝不复用他游戏的**。**这是本作唯一认真的广告收入。**

### 金币经济闭环（v3 缺，红队 S6：撤销 300 而通关只给 20-40 ⇒ 一次撤销 = 打 10 关）

| 产出 | 值 | 消耗 | 值 |
|---|---|---|---|
| 通关（按星） | 20 / 30 / 50 | **撤销** | **100** |
| 每日谜题完成 | 50 | **换一手（刷新）** | **50**（另有：每 8 次落子免费充能 1 次） |
| **无尽/每日/挑战结算（1.0.1）** | **score/100，封顶 100**（中位局 2971 分 ≈ 29 币，与通关同量级；封顶防马拉松局刷币） | **金币皮肤** | neon 800 / sakura 1200 / **aurora 1600 · volcano 2000 · galaxy 2600 · cotton 3200（1.0.2）** |
| 章末宝箱（30 章） | 150 起、每章 +25、封顶 800 | **金币买激励位给的东西（1.0.2）** | 开局礼包 200 / 图鉴 +5 张 150 / 🧱 送方块 250 |
| 结算「看广告×2」（1.0.1） | 刚结算那笔再给一遍；去广告玩家**不看广告直接双倍** | | |
| SWEEP / PERFECT | ~~2 / 20~~ ⚠ 未单独接（SWEEP 加分已通过「分数→金币」间接产币，单独发币成了重复奖励——暂缓） | | |
| 激励视频 | 25 | | |
| 成就 | 10-100 | | |

金币 `+` 按钮 → 看激励视频领币（P4 可加金币包 IAP）。**⛔ 绝不做「看广告提现赚钱」型换皮**（该品类已有多起诈骗投诉，会拉低整个产品信任）。

**⚠ 1.0.1 前的结构性破洞（已修，别再挖回来）**：非去广告玩家的道具永远走「看广告」、皮肤又全是星星解锁 ⇒ **金币对他们没有任何消耗出口**，「看广告领币」发的是一种花不出去的货币 = 死广告位。修法 = 金币皮肤（上表）。**新增任何产出前先问一句：这币花到哪去？**
**⚠ 1.0.2 又补了一次同款**：两款皮肤（2000 币封顶）在中期就买光了，金币重新开始溢出 ⇒
加 4 款高价皮肤（顶到 3200）+ 让金币能买激励位给的东西。**查法**：把「每天能挣多少」和
「所有出口加起来能花多少」摆在一起算一遍。

### 9.2 激励视频八个位 + 每日额度（1.0.2 定稿）

数值全在 `js/shop.js` 的 **`AD_CAPS` / `AD_REWARD` 两张表**里，改数值只动那两行。

| 位置 | 奖励 | 额度 |
|---|---|---|
| 🏪 商店「领币」 | +25 🪙 | 5/天 |
| 🎁 每日礼物（HOME） | +60 🪙 + 2 👼 | 1/天 |
| 👼 图鉴加速 | +5 👼 | 3/天 |
| 🎨 皮肤解锁 | 永久解锁下一款（先给盘数款，再给金币款） | 1/天 |
| 📋 任务加速 | 直接完成一个今日任务（奖励与自己打完**完全一致**） | 2/天 |
| 🚀 开局礼包 | 本局 +1 免费撤销 + 2 次换手充能 | 1/天 |
| 🧱 **送方块** | **接下来 2 手托盘全是 1×1**（共 6 个单格块） | 3/天 |
| 💡 提示 | 最优落点 | 每局首次免费，之后 3/天 |
| （结算金币 ×2） | 刚结算那笔再给一遍 | 每局 1 次 |

- ⛔ **关卡失败仍然零广告**（红线不变，E2E 扫描失败界面断言没有任何 AD/REVIVE 按钮）。
- ⛔ **拒绝 ⇒ 什么也不发生**（不发奖励、不扣额度、不惩罚）；**去广告玩家所有位直接给**，但照吃额度。
- ⛔ **额度是设计不是抠门**：没有它，长线收集当天就能被刷穿，而且**线上收不回来**。
  ⚠ 跨天重置必须**按 `AD_CAPS` 全量清**（手写清 key 必漏）。
  ⚠ **加新位时回头查一遍老位有没有 cap** —— 「看广告领币」上线一年零额度、可无限刷，就是这么漏的。
- 冒烟 `tests/e2e-rewards.cjs` 逐位钉死四件事：真到账 / 拒绝零发放且不扣额度 / 用完不再发 / 跨天全量重置。

### 9.1 插屏总闸门（2026-07-31 定稿，取代早期「每3次通关/四重护栏」两套规则；红线三条不动）

**定位明确了：口碑优先，插屏近乎象征性存在；这个方案下收入主力 = 自愿的激励视频。**

- **前 50 盘零插屏**（关卡/无尽/每日/挑战都算盘）≈ 中位玩家（2.7 盘/天）**前 18 天无广告**。这是明面卖点：商店页/公平页直接印「前 50 盘绝无插屏」——没有头部产品敢写。
- 之后**全局共享「每 10 盘至多 1 个」的预算**（`Shop.AD_GATE`，单一闸门 `canShowInterstitial`），只出现在两种正反馈/转场时刻：**关卡通关结算**、**无尽点「再来一局」的转场**（绝不盖死亡瞬间）。
- **距上次任何插屏 ≥2min**。
- ⛔ 失败 / 局中 / 每日谜题结算 / 挑战局**永远零插屏**（红线 3 原样）。
- 账目（自知之明）：D30 留存约 5% ⇒ 九成玩家一辈子看不到插屏；过门槛玩家 ≈0.27 次展示/天。**插屏是姿态，不是收入**——收入看激励视频（换一手/撤销/领币/结算×2，全自愿，拒绝零惩罚）。
- **IAP「去广告」封存不接**（2026-07-31 决策）：这个频率下「想付钱去广告」的动机基本消失，且省掉 RC dashboard 手工步。ASC 的 `cubeblast_noads`（READY_TO_SUBMIT）放着不提交，将来想上随时能上。⚠ 原生商店页的「Remove Ads」按钮已撤（假按钮比没有按钮更伤信任）；`wallet.noAds` 历史开关继续兑现，不收回。

---

## 10. 减压机制预算表（红队 S7：v3 的善意机制叠了 11 层，游戏会不会死）

**只留 4 项**，其余全砍：

| ✅ 留 | ❌ 砍 |
|---|---|
| **下一组预览**（可见的公平，且可关） | ~~保证至少一块可放~~（§0.2 假机制） |
| **每局 1 次免费撤销** | ~~DDA / pity 直条~~（§0.3） |
| **失败零惩罚**（一键重开） | ~~兜底强塞 1×1~~ |
| **streak 有 1 次零消除的宽限** | ~~关卡失败看广告续命~~（§6.2） |

---

## 11. 进度 / 留存 / 4.3 外壳

### ⛔ 11.1 App Store 4.3 是最大的上架风险（红队 F5 —— v3 只防了商标）

4.3(a) 打的不是名字，是**东西本身**：一个明确声明"纯克隆玩法"的 8×8 block puzzle **正是 4.3 的靶心**。abyssshoot 能过是因为有肉眼可见的差异层（鱼图鉴/合成）。而 v4 的两个差异点（块流公平、SWEEP）**审核员在截图和 30 秒试玩里一个都看不见**。

**必须在 P1 就规划一个"审核员 5 秒内能看见"的外壳**（本仓家风 = snake gallery / mines codex）：
**水晶图鉴 + 主题演进** —— 关卡推进解锁不同"矿脉"章节（视觉主题整套换：糖果 / 深海 / 星空 / 遗迹），收集到的水晶进图鉴（带描述）。这层必须进**商店截图第 1-2 张**。
**P4 之前必须回答**："打开 App 30 秒，这游戏和 Block Blast 长得像不像？" 答案必须是"不像"。

### 11.2 其余元层

- 章节地图（30 关 / 3 章），每章末**星星宝箱** → 金币 + 皮肤。
- **每日谜题**：日期做种子 ⇒ **全球同一条块流**（§0.4；v3 的做法做不到，红队 S8）。防作弊：改系统时间只影响本地记录，不影响榜。
- 皮肤 6 套（4 星星解锁 + 2 金币购买 neon/sakura —— 金币的消耗出口，§9）。`PAL` 切换，**渲染函数必须确定性、禁 `Math.random`**（snake 实踩）。
- 成就 ~30（数据表驱动）：最长 streak、单局 2 次 SWEEP、PERFECT CLEAR（稀有）、零撤销通关、连续 7 天…
- **只存**：关卡进度/星星/金币/皮肤/成就/最高分/图鉴/当局快照。**不存永久数值加成**（会毁掉分数可比性）。

---

## 12. 工程

### 12.1 分层（index.html 加载顺 = 依赖顺，load-bearing）

引擎 → `prng` → `pieces`（32 块 + 权重表）→ **`dealer`（纯函数 `stream(seed,i)`，无状态）** → `levels` → `core`（棋盘/放置/消除/计分/streak/SWEEP/game-over/收集/撤销）→ `storage` → `drag` → `fx`（§8.1，可整体关闭）→ `render` → `main`。

- **纯逻辑层**（双导出，node 可单测）：`prng / pieces / dealer / levels / core`。改这些**先写测试**。
- **`G.phase` 全集**（红队）：`menu / levelselect / tutorial / playing / dragging / paused / ad / win / lose / daily / settings`。⚠ **存档时 `dragging` 必须归一化回 `playing`**，否则恢复出"手指悬空"的畸形态（CLAUDE.md 那条"畸形存档 = 无报错白屏"）。
- **撤销栈**（1 步）必须含 **`streamIndex` + `seed`**（红队 S4：v3 漏了 PRNG 状态 ⇒ 撤销会变成"刷块外挂"——重新补牌时抽出不同的块）。块流是预生成的 ⇒ 回滚 `streamIndex` 即可，**天然免疫**。撤销时清空 `fx` 队列。
- **存档**（`CFG.key('run')`，带 `v:N`）：进度/星星/金币/皮肤/图鉴/成就 + 当局快照（棋盘/`seed`/`streamIndex`/分数/streak/目标/phase）。**改 `G` 形状必 bump `SAVE_VERSION`（旧档丢弃不迁移）；改任何 js/css 必 bump index.html 所有 `?v=N`。**
- **零硬编码文案**：`T('blockblast.xxx')` + `locales/<lang>.json`（**必须嵌套结构** —— 扁平写法查不到且零报错，snake 实踩）。⚠ **i18n 成本被 v3 低估**：公平页是长说明文（需要 canvas 可滚动长文视图，引擎没有）+ 30 关名 + 30 成就 + 4 个赞美词（要短要押韵，机翻会尬）。P4 单列工作项。

### 12.2 测试

- **node 单测**：放置/消除/计分/streak 边界（断裂需连续 2 次零消除、7 连封顶）/ SWEEP 三档阈值与 `cleared` 口径 / **game-over = 剩余的每一块都放不下**（且专测「消行救活卡住的块」的用例）/ **`stream(seed,i)` 与棋盘/分数无关**（§2 承诺 2 的可执行版）/ 权重抽样分布 ±1% / 水晶只在消除时计入 / 撤销精确复原（含 `streamIndex`）/ **关卡不可胜检测**。
- **`tools/sim.js`**（**直接驱动真 `js/core.js`**，不再自带一套平行规则 —— 否则模拟与游戏会悄悄漂移，数字就成了自欺欺人）：参考 AI **版本锁定**，改动需单独 review。`npm run sim:block` 一键跑。
  **回归基线（修正 game-over 判定后，600 局 mid）**：中位落子 **143**、中位分 **2971**、7 连满档率 **61.2%**、SWEEP 局占比 **23.7%**、PERFECT **0%**。
  （旧基线 93 落子 / 1903 分是**被错误的 game-over 判定污染的** —— 那时「任一块放不下」就判死，等于每局都被提前错杀。）
  判读线：**SWEEP 局占比 <15% ⇒ 招牌死了**；**中位落子 <40 或 >150 ⇒ 单局时长跑偏**。
  ⚠ v3 那个"三块全可放 60-80%"的指标**作废**（红队 S9：它测的是 AI 棋力，不是 dealer；实测 96%+，毫无区分度）。
- **`tools/verify-levels.js`**（§6.1）：每关 200 次 AI × 不同种子，通关率 <80% 不进包。
  **`npm run fix:levels`**（1.0.2）：生成 300 关 → 跑门禁 → 只把不达标的关降一档重生成 → 收敛后自动标定 par（§6.3）。
- **`tools/audit-sfx.cjs`**（1.0.2）：离线渲染 16 个音效为 wav + 峰值/静音/拖尾断言（§8.2）。**产物是给人听的**。
- **`tests/test-coach.js`**（1.0.2）：教练只读 / 妙手与失误判定 / 生存模拟 / 死亡复盘 /
  **性能基线**（`judge` <15ms、复盘 <1.5s）/ ⛔ **教练不破坏块流**（§2.1）。
- **`tests/e2e-rewards.cjs`**（1.0.2）：激励八个位的经济红线冒烟（§9.2）。
- **E2E**：拖拽落子（pointer 序列）、消行、SWEEP、过关三星、道具、存档续玩、语言切换。

### 12.3 合规 / 平台（v3 全缺）

ATT + UMP 同意流（`admob-monetization` skill）· App Privacy 标签 · 年龄分级 · **child-directed 判定**（block puzzle 用户画像易被判儿童向 ⇒ 广告须走 child-safe）· 离线时广告/IAP 的降级行为 · iOS 壳（`package.json` / `capacitor.config.json` / `resources/` + codemagic workflow 4 vars）。
**⚠ 本仓无后端 ⇒ 零埋点**：文档里所有留存/难度 KPI **没有线上测量手段**，只能靠 `sim.js` + 真人手测验证。别自欺欺人地写 A/B。

---

## 13. 美术

先**纯色方块 + emoji 水晶占位**（同 snake/mines 惯例）。风格：糖果紫渐变 + 高光斜角立体方块 + 圆润卡通。出图走 ComfyUI 本地 Flux（`game-art-pipeline` skill），`makeArt` + `drawArtIcon` 缺图回退 emoji，**零改码换图**。
⚠ 参考截图里的教授角色/头像/方块贴图**一律不得复用**，全部原创。

### 13.1 图标（已定稿，2026-07）

**画面 = 玩法本身**：一块**四格锯齿（S 型）电光拼块**高速斜插而下，盘面上有一个**同形空缺 + 金色虚线落点**，周围是品红/绿的立体方块 —— 「拿一块 → 塞进对得上的洞」这条规则不用文字就说完了。
（走过的弯路：只画「爆行」的方案全被毙 —— 爆炸只说明*有东西被消掉*，不说明*你要干什么*。**三件套缺一不可：悬空的块 + 同形的缺口 + 落点提示**。经验已回写进 `generating-app-icons` skill。）

- 主图 `Gemini_Generated_Image_z4k19az4k19az4k1.png`（2048²）→ `python ~/.claude/skills/generating-app-icons/gen-icons.py <src> --out games/blockblast --bg "#290A5A"`
- 产物：`resources/icon.png`(1024 RGB) · `resources/splash.png`(2732，底色取图本身的深紫 `#290A5A`，避免拼接边) · `assets/icons/{favicon-32,icon-192,apple-touch-icon-180,icon-512}.png`，已接进 `index.html` head（含 `theme-color`）。
- 三项硬校验全过（正方 ≥1024 / 无 alpha / 四角非白），苹果 squircle 遮罩 60px 实拍**无白缺口**。
- ⚠ 金色虚线落点在 60px 已不可见 —— 它服务的是**商店截图与 512 og:image**，不是主屏图标。别为了它去加大线宽（会把图标画脏）。
- 法务：块形（tetromino）是公有机制可用，但**不得套经典 Tetris 配色 + 从天而降的整体外观**（Xio v. Tetris Holding 打的是「整体视觉呈现」）。本图用糖果紫/青/品红，且是斜插入格，安全。图上**无任何文字** ⇒ 与商店名解耦（§15：商店名绝不能出现 "Block Blast"）。

## 14. 分阶段

1. **P1 核爽**：`pieces`(32 块表) + **`dealer`(块流)** + **`drag`(§5)** + `core`(计分/streak/SWEEP/game-over) + **`fx`(§8.1，独立工作项)** + 单测 + `sim.js` 回归。
   **验收（可操作版，红队）**：① `sim.js` 回归基线不劣化；② **3-5 个真人各玩 10 局，每次 game over 问一句「你觉得是自己摆错了还是发牌坑你」——「怪发牌」≤ 20%**。（v3 那条"我自己连玩 10 局感觉被坑 ≤1 次"是自我认证，作废。）
2. **P2 关卡**：水晶/目标/石块 + **`verify-levels.js`** + 不可胜检测 + 30 关 + 三星 + **前 5 关 FTUE**（§6.3）+ 图鉴外壳（§11.1）。
3. **P3 元层**：每日谜题 + 章节宝箱 + 皮肤 + 成就 + 公平页。
4. **P4 变现/上架**：去广告 IAP + Ads（§9 红线内）+ i18n 十语 + 合规 + iOS 壳 + 商店素材 + **原创商店名**。幽灵模式（§7）此时决定做或砍。

## 15. 法务 / ASO

- ⛔ 商店名/副标题/关键词**绝不出现 "Block Blast"**（商标）；目录代号仅内部用。
- ✅ **可用的描述性通用词**（非商标专有，是品类主流量入口）：`block puzzle` / `block puzzle game` / `grid puzzle` / `blocks` / `puzzle game`。取名走 `appstore-listing` skill。
- 玩法机制不受版权保护：**只抄机制/数值，代码全自写，美术/名称/文案/角色全原创**。
- ⛔ 不做假买量素材（"IQ 180" 型与真实玩法无关的广告）——调研显示招来的是失望用户 + 一星。
- ⚠ **4.3 相似性风险见 §11.1** —— 这是比商标更可能毙掉项目的东西。

## 16. 调研与验证来源

- **模拟器**：`games/blockblast/tools/sim.js`（本仓，§0 的所有数字由它产出，可复现）。
- **计分公式逆向**：[gary-z/blokie](https://github.com/gary-z/blokie)（Blockudoku 引擎，含逆向出的精确计分）· [Braindoku 官方 FAQ](https://murka.helpshift.com/hc/en/25-braindoku---sudoku-block-puzzle/faq/1383-how-is-the-score-calculated/) · [1010! 分析](https://blog.coelho.net/1010-analysis/)
- **8×8 克隆实现**：[tokaa1/blockerino](https://github.com/tokaa1/blockerino) · [RisticDjordje/BlockBlast-Game-AI-Agent](https://github.com/RisticDjordje/BlockBlast-Game-AI-Agent)
- **玩家爽点/差评**：[App Store 评论](https://apps.apple.com/us/app/block-blast/id1617391485?see-all=reviews) · [Metacritic（5.2/10、45% 负评）](https://www.metacritic.com/game/block-blast/user-reviews/) · [Woodoku ComplaintsBoard](https://www.complaintsboard.com/woodoku-wood-block-puzzles-b149599) · [Blockudoku 舆情（"广告过度"=#1 卸载原因）](https://marlvel.ai/intel-report/puzzle/blockudoku-block-puzzle-game)
- **留存/时长**：[Udonis 统计](https://www.blog.udonis.co/statistics/block-blast) · **变现拆解**：[Balancy](https://balancy.co/blog/2025/03/26/how-could-block-blast-by-hungry-studio-earn-more-monetization-and-gameplay-deconstruction/)
- ⚠ **不可信来源警示**：`blockblastsolverss.com` / `blastblocksgame.com` 等站声称"逆向 50 万次发块 / 有 pity timer"——**无原始数据、无代码，判定为编造**。
- ⚠ **证据缺口**：Reddit 全站不可爬；玩家原话来自 App Store/Play 评论与聚合站，可信度中等。

## 17. 仍需实机确认（不阻塞 P1）

1. 灰色 3×3 石块在**托盘**里出现（= 可放置的惩罚块？）；我们先按"棋盘预置的不可消障碍"实现。
2. 刷新充能规则（我们定"每 8 次落子充 1 次"）。
3. 原作真实的 combo 曲线（我们的 §3 是基于同品类逆向 + 模拟校准重构的，**不是抄原作**）。
