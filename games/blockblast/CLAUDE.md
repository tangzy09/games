# CLAUDE.md — games/blockblast

**Cube Blast: Block Puzzle**（内部代号 `blockblast`）：8×8 方块消除拼图。托盘给 3 块不可旋转的拼图，拖上棋盘，填满整行/整列即消除；三块全放完才补新三块；**剩余的每一块都放不下 = 结束**。root `CLAUDE.md`（引擎契约、部署铁律、git 纪律、iOS 流水线）先读，本文件只讲本游戏专属。

**规格是权威**：`DESIGN.md`（改核心前必查；数值全部由 `tools/sim.js` 跑模拟校准，不是拍脑袋）。

## 当前状态（2026-08-02）

**1.0 已上架（`READY_FOR_SALE`，7-13 过审）。** 线上 web：<https://blocks.ai-speeds.com>
（**领先 iOS 两个版本**：含 1.0.1 全部改良 + 1.0.2 的教练/激励八位/300 关/送方块/音效重做/画像去重）

**iOS 待出包**：`package.json` 还停在 1.0.1 ⇒ 出包前先 bump 到 **1.0.2**（与 ASC 版本一字不差，否则 build 挂不上）。
⛔ 出包/提交必经用户批准。

- **留存批（2026-07-31）**：①**每日任务**（`js/quests.js` 纯函数：dayNo 确定性生成 3 个轻任务，进度挂 consume 事件流，完成自动发 +30🪙+1👼，profile 只存进度）；②**连续奖励阶梯** 3/7/14/30 天（`Daily.streakReward`，断签清零重来）+ **金币补签**（恰好漏 1 天可花 100 币接回，`repairStreak`，结算页按钮）；③**统计页**（14 项终身数据；`achievements.settle` 新增 bestStreak/sweepsTotal 累计）；④**每日 Wordle 式分享**（SHARE_DAILY，日期+分数+同种子链接）；⑤**菜单目标提示条**（宝箱>任务>临近皮肤>临近连续奖励）；⑥**推送提醒** `js/notify.js`（19:00 每日 + 21:30 streak 保护、玩过即撤、默认关、开关在设置）；⑦**求好评** `js/rate.js`（幸福时刻=三星通关/破纪录，15 盘门槛+90 天冷却+3 次/年，调用即记账）；⑧**反馈** `js/feedback.js`（DOM 底部表单 → feedback.ai-speeds.com 共享 hub，离线入队 boot 补发）。⑥⑦要新二进制；⚠ 新依赖 local-notifications@^6.1 / in-app-review@^6 首次云端构建盯 npm/pod。
- **生成美术（2026-07-31，本机 Flux 管线首批 9 张）**：`assets/art/`——5 水晶（cry_*）+ 3 章徽（ch_candy/ocean/forest）+ 宝箱（chest），comfyui-flux-local 管线（schnell Q4 生图 → InSPyReNet 抠图 → 512webp）。接入走 engine `makeArt('art',[ids])` + `drawCrystalArt`（**缺图回退矢量 drawCrystal/emoji，零改码换图**）；用在目标条/图鉴/章徽（关卡 HUD）/宝箱条，**棋盘小格仍用矢量**（小尺寸矢量更脆）。⚠ 生图坑：prompt 带「badge/emblem」会诱发英文字（ch_candy 首版被画上 CHAPTER），重生成需去措辞+强化 no text。
- **天使榜（2026-07-31，DESIGN §7 幽灵追赶的落地形态）**：`js/ghosts.js` 20 个预设分数角色（200→20000，头像复用天使画廊图）。⛔ §7 红线照守：**明确是游戏角色，文案绝不称「玩家」**（单测钉死名字不含 player/玩家）。进度零存档（由最高分推导 `beatenCount`）；局中超越即 toast、结算页对比行（x/20 + 下一个差多少，点进全榜）、目标条挂「差 ≤600 分」的追赶目标。
- **广告模型（2026-07-31 定稿，取代此前所有插屏规则）**：**前 50 盘零插屏**（明面卖点，商店页 adPolicy 文案明示）→ 之后**每 10 盘至多 1 个**，只在通关结算 / 无尽「再来一局」转场，≥2min 间隔；失败/局中/每日永远零插屏。唯一闸门 `Shop.canShowInterstitial` + `notePlayed/noteAdShown`（盘数 = 关卡赢/输、无尽、每日、挑战都计）。**插屏是姿态不是收入，收入主力 = 自愿激励视频（×2/换手/撤销/领币）。**
- **iOS 1.0.1 待出包**（package.json 已 bump 1.0.1；⚠ 出包/提交必经批准）：
  - **ASC 1.0.1 版本载体已建（2026-07-31，PREPARE_FOR_SUBMISSION）**，并补上了缺失的 **marketingUrl = https://blocks.ai-speeds.com**（两 locale，已回读校验）。⛔ **为什么必须补**：AdMob 的 app-ads.txt 验证只认商店页的「Developer Website」(= 版本本地化的 marketingUrl 字段)，**不认「App Support」(= supportUrl 字段)**；1.0 只填了后者 ⇒ 商店页没有 Developer Website 那一行 ⇒ AdMob 无处可爬、Verify 永远失败。⚠ 该字段在 READY_FOR_SALE 版本上**锁死**（PATCH 报 409 STATE_ERROR「cannot be edited at this time」，supportUrl 同样锁），只能随 1.0.1 上架生效。**提交前还差 whatsNew（更新版必填）+ 新 build**。
  - **IAP `cubeblast_noads` 封存不提交**（ASC id 6796603142 元数据齐全 READY_TO_SUBMIT，放着；将来要上：随版提交 + 从 git 历史 b6c19aa 取回 `js/iap.js` + RC dashboard 建 app）。商店页「Remove Ads」按钮已撤（假按钮伤信任）；`wallet.noAds` 历史开关继续兑现不收回。**RevenueCat 不再需要 ⇒ 出包零手工步。**
  - **Game Center**：bundle 能力已开，榜 `cubeblast.endless.best` / `cubeblast.daily.best` 已建；CI 钩子 `tools/ios-extra.sh` 注入 entitlement（codemagic 模板加了通用钩子：游戏目录有 `tools/ios-extra.sh` 就跑）。⚠ 新依赖 `@openforge/capacitor-game-connect@^1.1` 首次云端构建盯 npm/pod 两步。

- **🏠 主界面（2026-08-01）**：新 `HOME` phase = **启动落点/门面**。原来的 MENU 一屏塞了
  章节页签 + 30 关网格 + 宝箱 + 每日 + 无尽 + 四个 tab + 三个小钮 + 目标条 —— 功能全，
  但一眼看过去像设置页。现在 **HOME 只留门面**（天使 hero + 一个主按钮 + 六个带角标的入口），
  **MENU 原样保留成「关卡地图」**，从 HOME 的「关卡」格子进，功能一样没少。
  构成：hero（玩家最近解锁的天使；零解锁时回退画彩色方块）→ 标题 + 「出块序列开局前就定死」→
  收集卡（天使 n/500 + 条 + ★星星 + 金币）→ **智能续继主按钮**（无尽局没打完 ⇒「继续 4321」）→
  每日谜题（带 🔥streak）→ 2×3 入口（关卡★n / 天使 n / 成就 n/38 / 任务 n/3 / 皮肤 n/16 / 统计）→
  底部四钮（商店/图鉴/公平/设置，**图标配文字** —— 15px 纯 emoji 认不出来）。
  ⚠ 导航变了：`e2e-p2` 的「起手在菜单」断言已改成钉新策略（起手在 HOME → 点「关卡」进地图）。
  E2E：`npm run test:block:home`；`tools/shot-notch.cjs` 已加 `blockblast-home` 一屏。
- **🗺 关卡地图瘦身 + 结算卡 + 共享图标（2026-08-01，「MENU 和主界面重复了」）**：
  - **MENU 现在只做选关**：返回 HOME + 章节页签 + 10 关 + 章末宝箱。原来还画着每日/无尽/
    成就/皮肤/公平/设置/商店/图鉴/目标条 —— **HOME 上一模一样有一份**，两屏重复才是它
    「像设置页」的病根。⇒ **子页面的 `backButton()` 改成回 `HOME`**；局内 HUD 的 ‹Menu
    关卡局回地图、其余模式回 HOME；「新开一局 ↻」入口挪到 HOME 主按钮右侧
    （**别删**：不然有未完局时想重开没有出口，`e2e-p1` 有断言钉死两个入口同时在）。
    `e2e-home` 新增断言：地图上**不许**再出现 HOME 的六个入口。
  - **结算浮层 = 一张不透明卡**（`settleCard`/`drawRows`/`cardBtn`）：原来文字直接飘在棋盘
    上方，彩块从按钮边上透出来一片花。关卡胜/负 + 无尽/每日结算**共用同一张卡**，
    行高先量后画（`row(h, fn)`），矮屏走 `cmp` 收一档字号 —— 不再用 `SH * 0.32` 这种写死比例。
  - **系统 emoji → 共享 UI 图标库**（星/锁/金币/宝石/奖杯/日历/火苗/画框/皮肤/统计/商店/激励视频…）：
    引擎新增 **`makeUIArt(ids)`**（`engine/canvas.js`）—— `engine/ui-icons.js` 是 DOM 版
    (`<img>`)，canvas 游戏用不了。同 `makeArt` 的 `{load,get}` 形状 ⇒ `drawArtIcon` 直接复用、
    缺图仍回退 emoji；路径运行时从 engine 脚本标签反推（网页 `../../engine/` vs iOS 包 `engine/`）。
    `tools/check-ui-icons.cjs` 已加 `makeUIArt([...])` 的名单校验（拼错不会报错，只能静态查）。
    ⚠ 两个实拍抓到的坑：**浅色贴纸图标压在半透明白按钮上糊成一团**（底色改深）；
    **图标和文字必须分开量宽**（把 emoji 拼进字符串靠 `measureText` 猜位置，换图标后必然叠字）。
  - **子页面（成就/皮肤/商店/日历/图鉴/天使/任务/统计/天使榜/设置）的 emoji 也全换了**：
    页头统一走 `pageTitle(icon, emoji, label, y)`，行内价签/锁/看广告/火苗各自配图标。
  - **主视觉每次进 HOME 换一张**（`G.heroIdx`，从已解锁的天使里随机；`renderAll` 里离开 HOME 就清空 —— renderHome 每帧都跑，每帧重抽会疯狂闪）。
  - 验收：`node games/blockblast/tools/shot-ui.cjs` —— 一次截主界面/地图三章/胜三星/胜一星/
    失败/无尽结算 × 两种屏（414×896 + 360×640），先注入进度存档，产物 `C:/tmp/blockblast/ui/`。
- **🏠🗺 门面 + 300 关 + 送方块（2026-08-02，✅ web 已上线；iOS 随下个包）**：
  - **主界面可爱化**（用户：「做的可可爱爱的，类似 snake」）：canvas 版的天国开场 ——
    `drawHomeDeco()` 极光三带 + 26 颗各自节奏眨的星 + 两层交叠云海，`drawHalo()` 给 hero
    加圣光光晕 + 缓慢转的 12 道光芒。⚠ 位置一律**确定性散列**（⛔ 禁 `Math.random`，每帧重抽会乱跳）；
    ⚠ 动效受设置里「粒子/动态」门控，关掉就用固定时刻画一张静止的漂亮背景；
    ⚠ HOME 因此进了 `pulseActive()` 逐帧重画（关掉动效就不烧电）。
  - **🗺 关卡提成和 ▶ 一样大的第二颗主按钮**（用户点名）。原来它只是六格里的一小格 ——
    关卡是主玩法之一，不该那么小。腾出的那格换成**原来根本没有入口**的天使榜。
  - **⭐ 30 关 → 300 关 / 30 章**：`tools/gen-levels.js` 的 1-30 关保留手工配方（FTUE 与三章教学），
    **31-300 由 `autoSpec(id)` 按难度曲线程序生成**，曲线只在已知安全区里走（线数 ≤4 / 石块与线数
    联动封顶 / pieceCrystals 的 goal 只取 2-3）。⚠ 生成器现在**直接写回 `js/levels.js`**（`--write`）。
    新增 **`npm run fix:levels`**（`tools/fix-levels.cjs`）：跑门禁 → 找出 <80% 的关 → **只把那几关
    降一档重生成** → 再跑，收敛后**自动跑 `--write` 标定 par**。实测 3 轮收敛、300 关全部 ≥80%。
    ⛔ 两个坑：① **gen 生成的关没有 `par` 字段**，而 `starsFor()` 在 `!par` 时直接返回 1 星 ⇒
    忘了标定就是「300 关三星系统整个失效」，且没有任何测试会报错；② 修复循环**别拿显示的百分比
    当判据**（`toFixed(0)` 会把 79.5% 显示成 80%，循环以为过了、verify 自己判不过，卡在第 298 关）。
    地图的章节选择器从「3 个并排页签」改成 **‹ 当前章 ›**（30 个页签不可能并排）；
    成就补了 lvl60/150/300 + star300/600/900 六条长线档（原顶档 lvl30/star90 在第 3 章就封顶了）。
  - **🧱 送方块（新激励位）**：看广告 ⇒ **接下来 2 手托盘全是 1×1**（共 6 个单格块），盘面越满越救命。
    ⛔ **它绝不碰块流**：`Core.grantBonusHands` 期间 `streamIndex` 一动不动，用完之后从**原来那一块**
    继续 —— 一块不差、一块不换 ⇒ `stream(seed,i)` 的公平承诺一个字都不用改（`test-core` 两条断言
    + `e2e-rewards` 里 `streamIndex === before` 钉死）。⛔ 每日/挑战局禁用（同种子分数必须可比）；
    撤销要把礼包手一起回滚（否则撤销能刷礼包手）；礼包手的块**不驮拼块水晶**（它不在流里，序号会撞）。
    局内道具条因此从 3 格变 **4 格**：撤销 / 换手 / [🚀开局礼包→🧱送方块] / 💡提示。
- **🔊 音效打磨（2026-08-02，✅ web 已上线）**：`js/sound.js` 从「6 个单振荡器 blip」重做成一套小合成器。
  - **总线**：master → **压缩器** → destination，外加一条并行**混响**（IR 也是合成的白噪声衰减，零素材）。
    ⚠ **短促的 UI 音（落子/点击/非法）必须走 `dry: 1` 绕开混响** —— 0.9s 的混响尾挂在 0.05s 的点击上，
    每次操作都"嗡"一下，连点就糊成一片（audit 量出来 place 发声 0.49s，改后 0.08s）。
  - **响度**：原来各音峰值只有 0.03~0.29，玩家得把系统音量开到很大 ⇒ master 推到 3.2 让压缩器兜底
    （标准响度链路，不是各处调 gain 猜）。现在最响的 PERFECT ≈0.72、最轻的 tap ≈0.12，阶梯由工具量。
  - **6 → 12 个音**：原来收水晶/领奖/任务完成/榜上超越**全在复用 `sweep('sweep')`** ⇒ 听感上"什么都一样"。
    新增 `coin`（金币叮当，n 越多越密）· `collect`（玻璃质感）· `brilliant`（妙手）· `levelUp`（解锁，
    原来**完全没有声音**）· `heartbeat`（濒死氛围，视觉早有、听觉一直空着）· `tap`（菜单按钮，
    原来整个菜单/商店/图鉴**一个反馈音都没有**）。
  - **每次播放微随机**（±1.2% 音高 / ±6% 时长）——同一个音听两百遍只剩噪音感（snake 实锤）。
    ⚠ `Sound.setJitter(false)` 关掉它，离线渲染要确定性。
  - ⭐ **`AudioContext` 可注入** ⇒ 同一份代码能跑在 `OfflineAudioContext` 上：
    **`node games/blockblast/tools/audit-sfx.cjs`** 把 16 个音渲染成 wav 到 `C:/tmp/blockblast/sfx/`
    （外加一个 `all.wav` 顺序播全部），并断言**峰值不削顶 / 不是静音 / 拖尾不超时**。
    ⛔ 机器只能挡这三类翻车，**好不好听必须真的听一遍** —— 音效是典型的「测试全绿也可能很难听」。
- **🔍 教练 + 变现加厚 + 去重批（2026-08-01，✅ web 已上线；iOS 随下个包）**：
  - **⭐ `js/coach.js` —— 把验关卡的求解器搬进运行时**（本作最独有的一层）。原来那套参考 AI
    只跑在 `tools/verify-levels.js`（构建期算通关率），玩家一辈子见不到；现在同一套东西长出四样：
    **💡 提示**（局内第三个道具位，`Coach.best` 算**这一手的最优落点**，每局首次免费、之后广告）·
    **🔍 死亡复盘**（结算卡一行「第 37 手换个位置还能再走 12 步」，`postmortem` 对最近 3 手各跑
    top-4 候选的生存模拟）· **✨ 妙手**（落子前 `judge`，同消 2 条/收水晶且无更优 ⇒ toast）·
    **📉「我的弱点」页**（`WEAK` phase，两类**纯逻辑可判定**的失误：放着能消的行不消 / 一手造出
    ≥2 个孤格；累计在 `profile.brilliants` / `profile.faults`）。
    ⛔ **公平承诺一个字没动**：教练只看当前盘面 + 公开块流，`dealer.js` 的签名不变，
    `test-coach.js` 有一条测试专门钉死「疯狂使唤教练之后，同种子块流逐块不变」。
    ⚠ 两个实测数字（写进测试当基线）：`judge` ≈ 0.1ms（每手都跑，必须远快于一帧）、
    复盘 ≈ 4ms（死亡动画播放期间异步跑）。⚠ **顶尖手带 tag 也不判失误** —— 求解器自己都把
    「不消行」排进前 15% 时，我们没资格说他错（只是不表扬）。
  - **激励视频 4 位 → 8 位 + `AD_CAPS` 每日额度**（`shop.js` 两张表即全部数值）：
    🏪 领币 5/天 · 🎁 每日礼物 1/天（HOME 每日谜题右侧）· 👼 图鉴加速 3/天 · 🎨 皮肤解锁 1/天 ·
    📋 任务加速 2/天 · 🚀 开局礼包 1/天 · 💡 提示 3/天 · 结算金币×2（每局一次）。
    ⛔ **原来「看广告领币」零 cap，理论上可无限刷** —— 这是本批修的实锤洞。
    ⛔ 关卡失败**仍然零广告**（不做复活位，E2E 照旧扫描断言）。noAds 玩家所有位**直接给**但照吃额度。
    冒烟 `tests/e2e-rewards.cjs`（逐位钉死：真到账 / 拒绝零发放且不扣额度 / 用完不再发 / 跨天全量重置）。
  - **金币出口扩容**：+4 款高价金币皮肤（aurora 1600 / volcano 2000 / galaxy 2600 / cotton 3200，
    共 20 套）+ 金币买开局礼包 200 / 图鉴加速 150。原来出口只有 undo 100 / refresh 50 / 两款皮肤 /
    补签 100 ⇒ 中期金币必然溢出，**溢出的货币 = 死货币，领币位当场变死位**。
    ⚠ 广告解锁皮肤**先给盘数款**（本来白送，只是提前）再给金币款；**星星皮肤永不入池**
    （星星是三星通关的兑现，卖掉等于把关卡奖励作废，单测钉死）。
  - **等级 / 称号 XP 条**（`engine/meta.js`，第三份实现 ⇒ 按老规矩抽进引擎）：
    HOME 收集卡下面一行 Lv + 称号 + 紫色 XP 条，点进统计页。xp 由 `Achievements.xpOf` 折算
    **既有计数器**（落子 1 · 消行 4 · 收集 5 · 妙手 8 · SWEEP 15 · 星 20 · 通关 60），**零存档**。
  - **⛔ 天使画像去重（-26MB）+ 顺带修好线上的空图鉴**：本目录那份 `assets/angels/` 是 snake 那份的**第二份拷贝**，已删。
    ⚠ **它其实早在 `023c965`（另一会话做 solitaire 时）就被 `git rm` 掉了，但代码里的路径没跟着改**
    ⇒ 从那次提交起，**线上 web 版的图鉴和主界面 hero 一直是空白占位**（本地和 iOS 包因为文件还在磁盘上而看不出来）。
    典型的「删了素材没删引用」，功能测试全绿、只有**看图**才抓得到。
    统一走 `engine/angels.js`（网页 `../snake/assets/angels/`、出包走 package.json 的 `wwwExtras`），
    顺序 = manifest 固定 seed 洗牌，和 solitaire 一致。⚠ 顺带修了一个真 bug：零解锁时 `heroIdx`
    抽到 -1 会**一直粘着**，在主界面领了礼物也不刷新（`< 0` 那个判据不能少）。
  - **♿ 5 种水晶不再只靠颜色**：一色一形（蓝菱形 / 粉六边 / 橙三角 / 绿圆角方 / 紫五角星），
    生成图那条路径也在右下角叠形状角标 —— 它们是关卡的**收集目标**，灰度下必须分得清。
- **玩法**：无尽模式 + **30 关 / 3 章节**（糖果瀑布/深海矿脉/翡翠林地，章末宝箱 150/200/300 币）+ 每日谜题（**日历页可补玩过去 7 天**，补玩不计 streak）+ 38 成就 + **16 皮肤（4 星星 + 2 金币 + 10 盘数白送 2~40 盘阶梯）** + **水晶图鉴**（5 种水晶）+ **天使画廊 500 张收集**（素材=language-study「大头萌天使」词图均匀抽样 `assets/angels/a001-500.webp` 24.5MB；顺序解锁、钱包只存一个数 `wallet.angels`；每盘+1/通关+2/破纪录再+1；render 有 LRU 64 张图缓存防低端机爆内存）+ 撤销/换一手道具 + 种子挑战。
- **拼块自带水晶（1.0.1，DESIGN §6 的另一半）**：第三章引入 `pieceCrystals: {every, kind, goal}` —— 驮不驮由 `Core.pieceCrystalAt(s, pieceIdx)` 纯函数决定（只看 seed+序号，公平承诺不破；撤销回滚同一块驮同一颗）。⛔ 带它的关**最多 1 颗石块**（2 颗会造行列双封死格 → 软锁死，validate 拦）。goal 别贪：首版 goal5+gap5 把 30 关打到 46% 通关率（verify 拦下），goal 2-3 才是甜区。
- **1.0.1 体验批（2026-07-31，web 已上线）**：⑧「无尽」按钮有未完局时=**继续**（原来一点就静默毁档）；⑨死亡序列（回放最后3手→逐块红色扫盘证明「都放不下」，点击跳过）兑现 DESIGN §2「失败可归因」；⑩濒死心跳(fill≥75%)/streak宽限橙闪/非法拖拽虚线描边/SWEEP·通关重震动——§8 欠的全还了；⑪关卡模式补迷你下一手预览；⑫设置页（预览开关=硬核模式、粒子开关=减弱动态）；⑬前2关首步指引（算出可消行的落点，托盘+落点脉冲高亮）；⑭成就分页（原来矮屏后半看不见）；⑮toast 防重叠排队。E2E 新增：继续/分页/设置持久化断言；p2/p4「造死局留孤格」彩票测试改全满盘（真踩响过）。
- **1.0.1 变现批（2026-07-30，web 已上线）**：①无尽结算产金币（score/100 封顶 100）+「看广告×2」（去广告玩家直接给双倍）；②无尽「再来一局」转场插屏，四重护栏 `Shop.AD_GUARD`（首日零插屏/短局<90s不出/距上次≥2min/每3局≤1个），**绝不盖死亡瞬间**；③通关插屏也吃首日+间隔护栏；④金币皮肤 neon/sakura（金币终于有消耗出口——之前非付费玩家的金币花不出去，「看广告领币」是死位）；⑤结算页重做（最长streak/清扫数/距最高分差距/NEW BEST 用 `G.newBestRun` 标志——老写法 `score>best` 在 best 更新后永远为假，从没显示过）；⑥破纪录**瞬间**toast庆祝；⑦堵掉「每日/挑战局写进 K_RUN 覆盖无尽存档」的老 bug（现在只有纯无尽局写 K_RUN）。
- **iOS**：Bundle `com.aispeeds.cubeblast`；ASC「**Cube Blast: Block Puzzle**」Apple ID `6790598746`；AdMob App `ca-app-pub-2141208066469648~6813744374`（激励 `/1692445484`、插屏 `/1094976854`，**本游戏专属，绝不复用他游戏的**）。
- **商店页**：en-US + zh-Hans；24 张截图（iPhone 6.7" + iPad 13" × 中英 × 6 张）。**第 1 张是公平页**——见下面 4.3 那条。
- **⚠ 内部代号 `blockblast` ≠ 商店名**。*Block Blast!* 是 Hungry Studio 的商标；商店名把 block 和 blast **分属不同短语、用冒号隔开**。别把代号写进任何面向用户的地方。

## 这个游戏的唯一卖点：预生成块流（别动它）

`dealer.js` 的全部公开面就一个纯函数：

```js
stream(seed, i) -> piece      // 第 i 块是什么，完全由 (seed, i) 决定
```

**签名里没有 board、没有 score** —— 想作弊都没有入口。这不是宣传语，是类型签名，`tests/test-dealer.js` 有一条测试专门钉死它（把棋盘填成快破纪录的样子、分数改到 99 万，同种子的块流必须逐块不变）。

它顺带白送了五样东西：**可验证的公平**（游戏内可查种子）、**下一手预览**（不存在"预览过的牌被偷偷换掉"）、**每日谜题**（同一天全球同一条块流，分数可比）、**撤销免疫刷块**（回滚 `streamIndex` 即可）、**种子挑战**（1.0.1：结算页分享 `?seed=` 链接，好友同一条块流比分；挑战局不写无尽存档、不进最高分）。

⛔ **任何"让发牌看一眼棋盘/分数"的改动都会同时毁掉这四样**，并把商店页上的公平承诺变成谎言。要改先想清楚。

## 被模拟/红队/实机推翻过的判断（别再走回头路）

- **「保证至少一块可放」是假机制**：每局只触发 0.04 次。死亡几乎从不是"三块全放不下"，而是"三块里有一块放不下"（三块必须全放完才补牌）。
- **DDA（按棋盘拥挤度调权重）测不出效果**（中位分差 1.5%），却正是玩家骂 rigged 的原型（"我盘面干净时它专门发大块"）。已砍。
- **game over = 剩余的每一块都放不下**，不是"任一块放不下"——先放能放的那块，可能消掉一行腾出空间，卡住的块就又能放了。（写错过一次，玩家一眼看穿。）
- **爽点是「每一步都有回报」（streak），不是「憋大招一次多消」**。计分公式以 streak 为主引擎。
- **新手救不了于发牌**：小块权重翻倍只让新手多活 4 步，却让熟手局长爆到 10 倍。只能靠教学（前 5 关预置盘面手把手喂一次连击）。

## 关卡：三道防线堵软锁死（红队标为致命）

石块永不消 ⇒ 含石块的行/列永远清不掉。若一颗水晶的**行和列都被石块封**，它永远收集不到，而玩家又**死不了** ⇒ 无限磨、既不 win 也不 lose。

1. `levels.validate()` 构建期拦（它当场抓出过我自己写关卡时的错）
2. `core.isUnwinnable()` 运行时兜底 → 判负 + **免费重开**
3. `tools/verify-levels.js`：每关跑 200 个种子的参考 AI，**通关率 <80% 不许进包**（第一版手写的 20 关里有 7 关不达标，最差的只有 3%）

⛔ **关卡失败零广告**，只给「立刻重来」——因运气打不过的关，出口若是广告，就是我们自己在 DESIGN §9 痛骂的东西。E2E 会扫描失败界面的所有按钮，断言没有任何 AD/REVIVE。

**关卡数据别手改**（300 关就更不可能手改了）：改 `tools/gen-levels.js` 的配方 / `autoSpec()` 的难度曲线，
然后 **`npm run fix:levels`** —— 它负责生成、把不达标的关降难度重生成到收敛、最后标定 par。
设计知识：**多条线的难度是超线性的**（4-5 线时 AI 通关率崩到 60%）；**石块是最强旋钮**（2 个石块封死 4 条线 = 通关率 -20%）。

## 常用命令

```bash
npm run test:block        # 单测（pieces/dealer/core/levels/meta/shop/coach）
npm run test:block:e2e    # 五个 E2E（P1 玩法 / P2 关卡 / P3 元层 / P4 广告红线 / rewards 激励八位）
npm run test:block:home   # 🏠 主界面（启动落点 / 智能续继 / 入口角标 / 刘海）
node games/blockblast/tools/audit-sfx.cjs   # 🔊 音效：渲染 16 个 wav 到 C:/tmp/blockblast/sfx（含 all.wav）+ 峰值/拖尾断言
npm run sim:block         # 手感回归基线：mid 中位落子 ~143 / 中位分 ~2971 / SWEEP 局 ~23.7%
npm run verify:levels     # 300 关通关率门禁（<80% 不许进包）；加 --write 标定 par
npm run fix:levels        # ⭐ 生成 300 关 + 自动把不达标的关降难度重生成，收敛后自动标定 par
node games/blockblast/tools/capture-shots.cjs   # App Store 截图（24 张）
```

## 本游戏踩过的坑（都真实发生过）

- **E2E 用 `dispatch()` 绕过真实点击 = 假绿**。菜单里每次点击都抛 TypeError（drag 层没查 phase），而 E2E 报"零 error"。**新加的 E2E 必须用 `page.mouse.click` 真点**。
- **顶层 `const G` 不会挂到 window 上**，而 render/drag 读的是 `window.G` → 渲染层拿到 undefined。单测永远抓不到这类跨文件全局约定问题。
- **模块顶层调用会写 `API` 的函数 = TDZ**（`Cannot access 'API' before initialization`），整个 render 模块挂掉、游戏白屏。
- **截图必须逐张肉眼验**（skill 的 HARD GATE）。它抓出了三个真实 UI bug，其中**消行预览一直是坏的**（高亮画在背景层、被实心块完全挡住）——而 DESIGN 里写着那是本作最重要的一个 UI。脚本报"生成成功"毫无意义。
- **广告红线是可测的**：E2E 断言「玩家拒绝激励视频 ⇒ 什么也不发生」（绝不强塞无奖励广告、绝不扣金币）、「局中零插屏」、「买了去广告后功能不变少」。
