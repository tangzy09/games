# connect4 P3：提示 / 复盘 / 精准度 / 妙手 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把已经算得出真值的求解器**变成玩家看得见的东西**——按一下就知道这步关不关键、下完能看到自己哪一手把必胜下成了必败、赢了输了都有一个能炫的数字。

**Architecture:** 全部建在 P1/P2 已有的层上：`js/solver.js`（真值，⛔ 一行不动）· `js/engine-client.js`（Worker 门面，`scores(moves)` 已就位）· `js/threats.js`（零搜索判据，妙手的便宜兜底走它）· `js/state.js`（`auto` 字段 P2c 已埋好，用来把限时局排除出精准度）· `js/render.js` + `main.js`（画）。**新增两个纯模块**：`js/review.js`（判分与转折点，**纯函数、零 IO、node 可测**）与 `js/analysis.js`（**边打边算**的调度与缓存）。**不碰求解器与真值层。**

**规格：** `games/connect4/DESIGN.md`（权威）。**动手前必读 §3.2、§3.3、§3.4、§4、§2.3、§2.4、§9.2（含 2026-08-06 新增的两段实测）。**

---

> ## 🚧 进度存档（2026-08-06）
>
> **工作目录**：`C:\tmp\connect4-p1`（git worktree，分支 `connect4-p1`）。⚠ 本分支尚未合并到 `main`，⛔ 未经明确指示别合。
>
> | 任务 | commit | 结果 |
> |---|---|---|
> | 前置测量 | `1ead067` `2aec96d` | 整局复盘 0.36-3.30 s · 倒序更快是错的 · 让子局是**奇偶性**问题（让 1 子 1.19 s / 让 2 子 **173.5 s**） |
> | 计划 | `767d7bb` | 本文件 |
> | **T1 判分层** | `e34deb5` | `js/review.js` 纯函数。判据是**胜负态不是分差**；变异实测确认 ③④ 各自独立承重 |
> | **T2 边打边算** | `fbcfe17` | `js/analysis.js`。E2E 实测**终局那一刻已算完 100%**、点击到落子中位 **21 ms** |
> | **T3 提示** | `5574829`（判据层）`524d7ef`（UI） | 分层两按已上界面。E2E 用 node 侧独立重算对拍「指的列真的是最优」。⭐ 判据层抓出「有几列不输」与「该走哪列」是**两个概念**（混用会让必败局面的提示指向**输得最快**的列） |
> | **T4 妙手** | `4c494b6` | 判据**复用 hintLevel1**（⛔ 不另立一套）。⭐ 两个判断被门禁逼出来：fx 的光环要 ≥2 格（妙手是一格）⇒ 改用金色 ✨ 条；借 fork 音当场污染 p2b-t5 ⇒ 新合成 brilliant.wav（语义区分：两条路=两个音 / 唯一解=一个音） |
>
> **`?v=` 仍是 13**（T7 收尾时统一 +1）。**`SAVE_VERSION` = 4**（P3 至今没动 `G` 的形状）。
>
> ### ⭐⭐ 已经踩过并修掉的三个坑（下一个动手的人务必知道，⛔ 别再踩一遍）
> 1. **库没就位就发 `scores`**（T2）：`analysis` 从 n=0 的前缀开始排队，而 `engine-client.js:209`
>    明写「库没就位时**绝不许**对 n≤9 调 `scores()` —— 那是**几十分钟**」。第一版没查 `bookReady()`
>    ⇒ 开局那几秒会把 Worker **焊死几十分钟**，画面上只是「复盘一直在转」，**零报错**。
>    ⇒ 已加 `bookOk()` 闸（fail-closed）+ `kick()`，单测 ⑥b 与 E2E ② 两层钉死。
> 2. ⭐⭐ **microtask 饿死输入事件**（T3，最难查的一个）：`pump` 在 `then` 里同步 `unshift + pump()`
>    ⇒ stale 恒真时转成 microtask 忙循环，而 **microtask 优先于宏任务** ⇒ 输入事件永远排不上。
>    **指纹是一组自相矛盾的现象：页面截得了图、`evaluate` 答得动、但 `mouse` 卡死**（e2e-p2b-t7
>    在结算屏点［再来一局］卡死 3 分钟）。⇒ 让出宏任务（`setTimeout 0`）+ `STALE_MAX` 封顶，
>    单测 ⑦b/⑦c 钉死。**⚠ 以后凡是在 `then` 里接力调度，一律用 `pumpSoon` 那条路。**
> 3. **退让顺序把玩家主动请求的内容挤没了**（T3）：最窄屏+舒适模式下提示条整条消失，而门禁
>    全绿（三个按钮确实都在屏内）。⇒ **只有逐张看图才抓得到**；已补机器断言（`G.hintRect` 非空）。
>
> ### ⏳ 未完成：T5 复盘页 · T6 精准度进结算 · T7 收尾
>
> **T4 起手可直接用的接口**（都已交付并测绿）：
> - 真值：`C4Analysis.get(moves)`（null = 还没算）/ `request(moves, {priority:true})` 插队；
> - 判据：`C4Review.hintLevel1(sa).kind === 'only'` 就是「只有 1 列不输」= **妙手的判据本身**，
>   `safeCols(sa)` 是那一列 ⇒ ⛔ 别再定义第二套判据。
> - 零搜索兜底：`C4Threats.forkOf/forPlayer`（main.js 里已有 `forkBlocked()` 的用法样例）。
>
> ⚠ 复核纪律（P2c 那一轮救过命的，仍然适用）：
> **裁决只认日志里的 `EXIT=` 行**（复合命令外层的 exit 是最后一条命令的码）；跑门禁前先 `cd /c/tmp/connect4-p1`。

---

## ⭐ 开工前已经量掉的三件事（⛔ 别重新推导，也别推翻它们的结论）

`tools/bench-review.js`（2026-08-06，本机；⛔ 每个变体独立进程）：

| 问 | 答 |
|---|---|
| 整局复盘要多久？ | **0.36 – 3.30 s**（四个 seed，25-39 手）。⚠ 方差 9 倍，**全看 n=10 那一个局面难不难**，与手数几乎无关 |
| 该不该倒着算（残局先算填表）？ | ⛔ **不该**。fwd 3.30 / rev 3.53 / 关表 4.41 s，差异在 §9.1 的 ±30% 噪声内。账的 2/3 是 n=10 那一次，它第一个算、没表可蹭 ⇒ **正序即可，别写任何排序复杂度** |
| 让子局呢？ | ⭐ **奇偶性**：让 **1** 子子数平衡 ⇒ 库照常命中 ⇒ 整局复盘 **1.19 s**（与正常局无异）；让 **2** 子不平衡 ⇒ 库 **100% 落空** ⇒ 整局复盘 **173.5 s**（最慢单个局面 **101 s**）⇒ ⛔ **让 2 子局（= 儿童档）绝不做整局复盘** |

⭐ 由此定下 P3 的性能架构，**Task 2 就是它**：

- **n ≥ 16 段几乎全是 0 ms** ⇒ 局中实时算精准度/妙手**是免费的**。§9.2 那条「延迟兑现」**只对 n=10..15 那一段必要**。
- ⭐ **边打边算**：每落一手就在 Worker 空闲时算那一手的真值，摊进玩家想棋 / AI 思考 / 落子动画的自然间隙 ⇒ **终局时复盘几乎瞬开**，总计算量一位不变。
- ⚠ 手机按 §9.1 要再慢 3-5 倍 ⇒ 最坏仍要 10 s 上下 ⇒ **进度必须可见**，且**绝不阻塞任何操作**。

---

## ⚠️ 四条贯穿全计划的红线

### 1. ⛔ 提示 / 复盘 / 悔棋 / 课程——永远免费，永不看广告
DESIGN §3.2 原文，且**已经是 E2E 断言**（P2c-T5 的门禁里就有「整个流程广告调用 = 0」这一条）。
竞品把提示做成 9 次限量道具——**这正是不学的东西**。⇒ 新增的每条 P3 门禁都要带这一条断言。

### 2. ⛔ 绝不谎报真值（§2.4）
求解器死了 / 开局库没就位 / 让 2 子局 ⇒ **如实说不给**（「本机算力不足，这局不显示精确评分」），
⛔ **绝不许返回一个「看起来合理」的分数或标签**。`engine-client.js` 抬头那段是这条的既有实现，照它。
⚠ 这条最容易破在**默认值**上：`accuracy = 0` 与「没算过」在 UI 上长得一模一样 ⇒ **用 `null` 表示没算过**，并让渲染分支显式处理。

### 3. ⚠ 措辞死线：陈述事实，⛔ 不指责（§3.3）
口径是「这一步之后局面变了」，**不是**「你犯了个错」。⛔ 全局不许出现「错」「失误」「blunder」的第二人称指责式表达。
⚠ 标签的**内部枚举名**可以叫 `blunder`（代码里要精确），但**面向玩家的那一层文案一律走 locale**，且措辞要中性。

### 4. ⛔ 公平承诺不许破坏
`aiMove(position, tier, seed)` 那条「入参里没有玩家状态」由**跨进程指纹测试**守着。
⚠ P3 会引入「玩家的精准度纪录」这类长期状态 —— ⛔ **它一个字节都不许流进 AI 的决策**，
也不许据此偷偷调难度（casual-game-meta 骂了整节的 DDA）。⇒ 收尾必须复跑 `test-ai-determinism.js`。

---

## Task 1：判分层 `js/review.js`（纯函数，先做这个）

**Files:** Create `games/connect4/js/review.js` · Create `games/connect4/tests/test-review.js` · Modify `package.json`（挂进 `test:c4`）

> §4：求解器给**每一手**打标签（最优 / 次优 / 失误 / 败招），一局算出 **Accuracy %**。

- [x] **Step 1：先定口径，⚠ 别急着写** —— 下面是**已经定好的方案**（理由附在后面，⛔ 别改成「按分差扣分」）：

  **判据是「胜负态」而不是分差。** `solver.js` 的分数约定：`>0 必胜 / =0 和 / <0 必败`，`|score|` 只表示**分出胜负的早晚**。
  ⇒ 把 `sign(score)` 归成三档 `WIN(+1) / DRAW(0) / LOSS(-1)`，**标签只看这一手把胜负态从哪档带到哪档**：

  | 标签 | 判据 | 计分 |
  |---|---|---|
  | `best` 最优 | 落在 `scoreAll` 的最高分列里（并列全算） | 100 |
  | `good` 次优 | **胜负态没变**，只是赢得慢了 / 输得慢了 | 85 |
  | `slip` 失误 | 胜负态**掉一档**（必胜→和，或 和→必败） | 40 |
  | `loss` 败招 | 胜负态**掉两档**（必胜→必败） | 0 |

  ⭐ **为什么不按分差扣分**：`|score|` 差 1 只是「晚一子赢」，那不是错误；而「必胜→和」哪怕分差只有 1 也是**把整局送掉了**。按分差扣分会把这两件事判反。
  ⭐ **副产品，而且是产品上想要的**：必败局面里**怎么走都不掉档 ⇒ 不扣分**。你不该因为对手完美而被判失误——这正是 §4 那句「输了但精准度 91%，是你的新高」能成立的前提。
  ⚠ **视角**：`scoreAll(落子前的局面)` 是**当前行棋方**视角，而当前行棋方就是**要落这一手的人** ⇒ 直接拿 `sa[实际落的列]` 与 `max(sa)` 比，两者同视角，⛔ 别取反。

- [x] **Step 2：TDD —— 先写 `tests/test-review.js`，⛔ 此时 `review.js` 还不存在**

  必须覆盖的用例（⚠ 每条都用**手摆的 `scoreAll` 结果**当输入，⛔ 别在单测里真调求解器：那样测的是求解器不是判分）：
  1. 四种标签各一条：`{3:5}` 单列必胜走 3 ⇒ `best`；`{3:5, 4:3}` 走 4 ⇒ `good`（同为 WIN）；`{3:5, 4:0}` 走 4 ⇒ `slip`；`{3:5, 4:-3}` 走 4 ⇒ `loss`。
  2. **并列最优全算 best**：`{3:5, 4:5}` 走 4 也是 `best`（⛔ 只认第一个 = 会把一半的好手判成次优）。
  3. **必败局面零扣分**：`{3:-5, 4:-3}` 无论走哪列都是 `best` 或 `good`，**不许出现 slip/loss**。
  4. **和棋局面**：`{3:0, 4:-3}` 走 4 ⇒ `slip`（DRAW→LOSS 掉一档）。
  5. `accuracyOf([...])` 空数组 ⇒ **`null`**（⛔ 不是 0 —— 红线 2：「没算过」与「0 分」必须能区分）。
  6. ⭐ **只算一方的手**：`accuracyOf` 要能只统计玩家那一侧的 ply（`{ side: 0 }`），⛔ 别把 AI 的手混进玩家的精准度。
  7. ⭐ **转折点**：`turningPoint(labels)` 返回**第一次**胜负态下滑的那一手（`{ply, from, to}`）；全程没下滑 ⇒ `null`。
     ⚠ 用「第一次」而不是「最严重的一次」：§3.3 那句话是「你到第 14 手为止一直是必胜的」——它讲的是**故事的转折**，不是扣分最多的那手。
  8. ⛔ **脏输入 fail-fast**：`scoreAll` 给了空对象 `{}`（终局局面）⇒ 抛错而不是返回一个标签（终局局面上没有「这一手多好」这个问题）。

- [x] **Step 3：跑测试确认它红** —— `node games/connect4/tests/test-review.js`，预期 `Cannot find module '../js/review.js'`

- [x] **Step 4：写 `js/review.js`**

  导出：`SIGN_WIN/SIGN_DRAW/SIGN_LOSS`、`SCORE_OF_LABEL`（那张 100/85/40/0 表，**导出是为了让门禁读同一份**，⛔ 别在测试里抄一份）、`labelOf(scoreAll, col)`、`accuracyOf(labels, opts)`、`turningPoint(labels)`。
  ⚠ 模块形状照 `clock.js`／`threats.js`：IIFE + `inNode` 双导出 + `Object.freeze(API)`。
  ⛔ 本文件**零 IO、零 Solver、零 EngineClient**（它只吃已经算好的 `scoreAll` 结果）——写进文件头，并在 Step 5 用源码级检查钉死。

- [x] **Step 5：加两条源码级红线断言**（照 `test-threats.js` 的先例）：本文件源码里不许出现 `Solver` / `EngineClient` / `Math.random`。
  ⚠ 「注释里写了纯函数」和「真的是纯函数」是两件事——本仓已经为这条写过一次检查，照抄。

- [x] **Step 6：挂进 `package.json` 的 `test:c4`**（⚠ 本仓铁律：不挂 = 永远不会被跑到），跑 `npm run test:c4` 全绿

- [x] **Step 7：Commit** —— `git add games/connect4/js/review.js games/connect4/tests/test-review.js package.json`

## Task 2：边打边算 `js/analysis.js`（P3 的性能地基）

**Files:** Create `games/connect4/js/analysis.js` · Create `games/connect4/tests/test-analysis.js` · Modify `js/main.js` · Modify `games/connect4/index.html`（加 script 标签）

> ⭐ 每落一手就在 Worker 空闲时算那一手的真值 ⇒ 终局时复盘几乎瞬开。

- [x] **Step 1：先想清楚它与既有三条纪律的关系，⚠ 写进文件头再动手**
  - ⚠ **AI 优先**：`EngineClient` 的 `send` 是「同一个 op 只认最新一次」——`scores` 这个 op 会被 P3 自己反复用，⛔ 别让复盘的排队请求把**提示**那次 `scores` 顶掉（两者同 op）。⇒ **排队必须在 analysis.js 这一层做**（一次只在飞一个），⛔ 不是甩给 EngineClient 并发。
  - ⚠ **撤销不作废**：缓存的 key 是**局面**（`moves` 前缀序列化，或 `Solver.keyOf` 的等价物），不是「第几手」⇒ 撤销只是不再需要后面那些，已算的前缀**照旧有效**。⛔ 别按 ply 号存。
  - ⛔ **让子局（1 子和 2 子都）直接关掉这条流水线**，判据 `C4State.handicapOf(g) > 0`。
    ⚠ 与 UI 显示「这局不给评分」的判据**必须是同一个函数**（照 `timedAllowed` 的先例，⛔ 别写两份）。
    **两条不同的理由，⛔ 别混成一条**：
    · **让 2 子**：性能上就不可能（前置测量：整局 **173.5 s**、单个局面 **101 s**）。
    · **让 1 子**：⭐ 性能完全没问题（**1.19 s**，与正常局无异），卡住它的是**协议**——
      `solver.worker.js` 的 `scores` op 走 `Bitboard.fromMoves(m.moves)`，即**从空盘重放**，
      ⛔ 根本表达不了预置子；而 Worker 只 importScripts 了 bitboard/rules/solver/book/ai，
      **没有 state.js** ⇒ 要支持就得二选一：改 P1 的 `bitboard.js`（连带它的门禁），
      或在 worker 里留**第二份**摆子逻辑（本仓明确反对）。
      ⇒ **收益撑不起成本**：让子只对「同机双人 + 轻松档」开放（§6.7），而同机双人局的
      个人精准度本来就意义不大。⇒ **P3 第一版不支持**，如实说明（红线 2），要做留到 P5。
  - ⚠ **零阻塞**：这条流水线**永远不许**让任何一次点击等它。它没算完 = 复盘页显示进度，⛔ 不是禁用按钮。

- [x] **Step 2：TDD —— `tests/test-analysis.js`**（⚠ 用**假 EngineClient**注入，⛔ 别在单测里起真 Worker）
  1. 喂 3 手 ⇒ 依次请求 3 个前缀局面，**一次只在飞一个**（断言假 client 的并发峰值 === 1）。
  2. ⭐ **AI 优先**：`request(优先级高的那次)` 插队后，下一次出队的是它。
  3. **撤销**：算完 5 手后撤到 3 手，再问前 3 手 ⇒ **命中缓存，零新请求**。
  4. ⛔ **让 2 子局**：`start({handicap:2})` ⇒ **一个请求都不发**，且 `disabledReason()` 返回可读原因（⛔ 不是空字符串）。
  5. **求解器死了**：假 client 一律 reject ⇒ 状态变成 `failed`、`disabledReason()` 如实说，⛔ 不许把 reject 吞掉当成「算完了」。
  6. ⭐ **进度**：`progress()` 返回 `{done, total}`，且 `done` **单调不减**（⛔ 撤销之后 total 变小时不许出现 done > total）。

- [x] **Step 3：跑测试确认红**，然后写 `js/analysis.js`，再跑绿

- [x] **Step 4：接进 `main.js`** —— 落子后 / AI 落完后触发 `Analysis.onMove(g)`；`startGame` / `goHome` 时 `Analysis.reset()`。
  ⚠ **别放进 `renderAll()`**（P2c-T5 那条教训：renderAll 每帧都跑，把有副作用的东西放进去会递归）。

- [x] **Step 5：⭐ 验收（E2E，`tests/e2e-p3-t2.cjs`）** —— 真实鼠标下一整局人机，然后断言：
  - 终局那一刻 `Analysis.progress().done / total` **≥ 80%**（证明「边打边算」真的摊掉了，⛔ 不是终局才开始算）
  - ⛔ 全程零 pageerror；⛔ 广告调用 = 0
  - ⛔ 每一次点击的响应都没被拖慢（量点击到落子的时间，与 P2b 的基线同量级）

- [x] **Step 6：Commit**

## Task 3：提示（分层两按，**永远免费**）

**Files:** Modify `js/main.js` · `js/render.js` · `locales/*.json` · Create `tests/e2e-p3-t3.cjs`

> §3.2 **第一按**：只说这步关不关键——「有 4 列都不输，随便走」vs「**只有 1 列不输**」。教育价值最高且不剧透。
> **第二按**：指出走哪一列 + 一句理由。理由从求解器评分结构**机械导出，不手写解说**。

- [x] **Step 1：第一按 = 数「有几列不输」** —— 判据：`scoreAll` 里 `sign >= 当前最优的 sign` 的列数。
  ⚠ **不是**「分数 ≥ 0 的列数」：必败局面里一列都不会 ≥0，那样会说「0 列不输」，而正确的话是「这局已经很难了」。⇒ 文案分三种：必胜局面 / 和棋局面 / **必败局面（如实说，⛔ 别假装还有救）**。

- [x] **Step 2：第二按的理由必须**机械导出**，⛔ 不手写解说** —— 只有三条，全部从已有数据得出：
  - 「唯一不败的一列」（不输的列数 === 1）
  - 「挡掉对方的双威胁」（走完之后对方的 `C4Threats.cells` 少了一个双威胁 ⇒ 零搜索可判）
  - 「做你自己的双威胁」（走完之后自己多了一个 fork ⇒ 同上，`C4Threats.forkOf` 已就位）
  - ⚠ 三条都不成立 ⇒ 就说「这一列最稳」，⛔ 别硬凑一个听起来聪明的理由（那是手写解说的开始）。

- [x] **Step 3：⛔ 红线断言** —— 提示按钮按下去 **广告调用 = 0**（写进本 task 的 E2E）。⚠ 且**不限次数**、不消耗任何货币。

- [x] **Step 4：⚠ 等待期间的诚实措辞** —— 提示要等 `scores` 回来（n=10 那次最坏 2 s）。
  ⇒ 转 spinner + §2.4 的措辞；⛔ 求解器死了要**如实说不给**，⛔ 绝不返回一个「看起来合理」的列。
  ⚠ 停表：限时局里按提示**必须停表**（P2c-T5 的 `clockBlock()` 里 `G.thinking` 那一支就是为这个留的，⛔ 别新开一个判据）。

- [x] **Step 5：⭐ 验收（真实鼠标）** —— 摆一个「只有 1 列不输」的局面，第一按必须说「只有 1 列」；第二按指的列**必须真的是** `scoreAll` 的最优列之一；⛔ 广告 0；⛔ 限时局里按提示表不走

- [x] **Step 6：Commit**

## Task 4：妙手 ✨（真值白送的传播件）

**Files:** Modify `js/main.js` · `js/fx.js`（复用既有 fork 光环的形状）· `js/render.js` · `locales/*` · Create `tests/e2e-p3-t4.cjs`

> §3.4：求解器知道一个局面**有几列不输**。**只有 1 列不输、而你找到了** ⇒ 当场弹 ✨妙手。

- [x] **Step 1：判据 = 「不输的列恰好 1 列」且「玩家走的正是那一列」** —— 数据 Task 1/2 已经有了，⛔ 别再定义第二套判据。
- [x] **Step 2：⚠ 时机问题（这是本 task 唯一的难点）** —— 妙手要**当场**弹才有爽感，而 n=10..15 段真值要 1.7 s（§9.2 点名的正是这条）。
  ⇒ 两段式，**照 §9.2 的裁定**：
  - **n ≥ 16**（实测 ~0 ms）⇒ **实时弹**。
  - **n ≤ 15** ⇒ ⛔ **别等**（等 = 弹在玩家下一手都想好之后，爽点全丢）⇒ 记账，**赛后复盘时一次性兑现**（「这局你走出了 2 个妙手」）。
  ⚠ 两条路的**判据必须是同一个函数**，⛔ 别写两份（一份实时一份赛后 = 两个数不一致，而且没人会发现）。
- [x] **Step 3：复用既有 fork 光环的动画**（P2b-T5 已有），⛔ 别重造；进减弱动态门控（§6.8）。
- [x] **Step 4：⭐ 验收** —— 摆一个「只有 1 列不输」且 n≥16 的局面，走对 ⇒ 当场出 ✨；走错 ⇒ **不出**（反向对照，⛔ 别只测出的那一半）；⚠ 连续两手都是妙手时不刷屏（照 P2b-T5 `forkCount` 那条门禁的写法）
- [x] **Step 5：Commit**

## Task 5：赛后复盘页（**最大的差异化**）

**Files:** Modify `js/render.js` · `js/main.js` · `locales/*` · Create `tests/e2e-p3-t5.cjs`

> §3.3：**胜负曲线** + **转折点**（「你到第 14 手为止一直是必胜的。第 14 手走了第 3 列——这一步之后变成必败。当时该走第 5 列。」）+ **［从这一步重来］**

- [ ] **Step 1：入口在结算屏**（⛔ 别做成一进 HOME 就弹）。⚠ 返回键左上角（本仓铁律，`safeTop` 起算）。
- [ ] **Step 2：胜负曲线** —— 用 Task 1 的 `sign` 序列画，⚠ **不是**画 `score` 原值（`|score|` 的锯齿会让曲线看起来在剧烈震荡，而胜负态其实一直没变）。
  ⚠ canvas 不自动换行 ⇒ 所有文案过 `txtLWrap`（德/俄会溢出）。
- [ ] **Step 3：转折点文案** —— 走 `turningPoint()`。⚠⚠ **措辞死线**（红线 3）：陈述事实、⛔ 不指责。
  ⚠ 「当时该走第 5 列」里的列号**必须来自那一手的 `scoreAll` 最优列**，⛔ 不许是「现在算出来的最优」（局面已经不同了，那是另一个问题的答案）。
- [ ] **Step 4：［从这一步重来］** —— 直接调 P2a 就有的 `C4State.rewindTo(g, n)`，⛔ 别新写一套。
  ⚠ P2c-T5 埋的坑要一起过：`rewindTo` 会按 `p < k` 砍掉 `auto` 归因 ⇒ 重来之后那一手不再顶着时钟的名字（已有实现，本 task 只要**别绕开它**）。
- [ ] **Step 5：⛔ 三种「不给复盘」的诚实分支** —— 全部如实写，⛔ 都不许显示一个编出来的数字：
  1. **让子局（含儿童档）**⇒ 「这局有让子，不做精确复盘」（两条理由见 Task 2 Step 1）
  2. **求解器不可用**（§2.4）⇒ 「本机算力不足，这局不显示精确评分」
  3. **还在算**（手机最坏 10 s）⇒ 显示进度，⛔ 不是禁用按钮、⛔ 也不是空白页
- [ ] **Step 6：⭐ 验收** —— 下完一局真局 ⇒ 复盘页出得来、曲线点数 === 手数、转折点那一手的「该走的列」与现场重算的 `scoreAll` 最优列**逐位一致**；⛔ 广告 0；三种降级分支各截一张图**肉眼看**
- [ ] **Step 7：Commit**

## Task 6：精准度进结算屏 + 纪录

**Files:** Modify `js/main.js` · `js/render.js` · `js/settings.js`（或独立存储键）· `locales/*` · Create `tests/e2e-p3-t6.cjs`

> §4：**「你输了，但这局精准度 91%，是你的新高」**——输局也能创纪录、也能庆祝。

- [ ] **Step 1：结算屏显示本局精准度**（只统计**玩家那一侧**的手，Task 1 的 `opts.side` 就是为它留的）
- [ ] **Step 2：⛔ 限时局不计入纪录**（§6.10 白纸黑字）—— 判据用 P2c-T5 已经埋好的 `C4State.timedOf(g)`，⚠ 而**时钟代落的那几手**（`auto`）**必须从精准度里剔除**：那不是玩家下的。⛔ 这两条是两件事，别只做一条。
- [ ] **Step 3：⛔ 让 2 子局同样不计入**（没有真值可算），⚠ 且如实说明而不是显示 0%
- [ ] **Step 4：纪录持久化** —— 存「最高精准度」。⚠ 若要进 `G` 就必须 bump `SAVE_VERSION`（本仓铁律）；**建议存 settings 侧的独立键**（它是跨局的玩家纪录，不是这一局的状态）⇒ 不必动 `SAVE_VERSION`。
  ⚠ 持久化断言走**非默认值方向**（P2c-T5 那条：默认值方向的断言在字段没进 `defaults` 时照样绿）。
- [ ] **Step 5：⭐ 验收** —— 输一局但精准度高 ⇒ 结算屏出现「新高」而**不是**「你输了」大字（§6.6 那条「让输不疼」不许被 P3 破坏）；限时局打完 ⇒ 纪录**没变**（反向对照）
- [ ] **Step 6：Commit**

## Task 7：收尾

- [ ] **Step 1：⛔ `?v=N` 统一 +1**（含 `engine/*.js`；⚠ 验收 `grep -o "?v=[0-9]*" index.html | sort -u` **只有一个值**）
- [ ] **Step 2：`node tools/check-locales.js games/connect4/locales` 0 fail**
- [ ] **Step 3：⭐ 全量门禁** —— `test:c4` / `render` / `worker` / `e2e` / `p2b` / `p2c` / 新增的 P3 门禁 / **六个游戏的 `npm test`**
  ⚠ P2a/P2b/P2c 各实锤一次：**只跑自己那条门禁不够**
  ⚠⚠ **裁决只认日志里的 `EXIT=` 行**（复合命令外层的 exit 是最后一条命令的码，恒 0）
  ⛔ **必须复跑 `test-ai-determinism.js`**（红线 4：P3 引入了玩家长期状态，跨进程指纹必须仍绿）
- [ ] **Step 4：文档回填**（`DESIGN.md` 阶段索引 P3 → ✅ · root `CLAUDE.md` · `README.md` · 本计划转历史记录）

---

## P3 完成判据

- [ ] 全量门禁 exit 0（含六个游戏）
- [ ] ⛔ **提示 / 复盘 / 悔棋 全程广告调用 = 0**（红线 1，E2E 断言）
- [ ] ⛔ **公平承诺没被破坏**（跨进程指纹仍绿；玩家纪录一个字节都没流进 AI）
- [ ] ⭐ **转折点指的「该走的列」与现场重算的最优列逐位一致**（⛔ 不是「看起来合理」）
- [ ] ⭐ **边打边算真的摊掉了**：终局那一刻已算完 ≥80%
- [ ] ⭐ **必败局面里不扣分**（输了也能有高精准度 —— §4 那条正反馈的前提）
- [ ] ⛔ **三种「不给评分」的分支全部如实说**（让 2 子 / 求解器不可用 / 还在算），⛔ 没有一处显示编出来的数字
- [ ] ⛔ **限时局与时钟代落的手不进精准度**（两条都要，§6.10）

## 不在 P3 范围

课程系统 → **P4**；Pop Out → **P1b**；元游戏/变现/上架 → **P5**；**美术** → 未启动
§3.3 提到的**分享战绩图卡**与 5 秒回放动画 → 留到 P5（它是传播件，和元游戏一起做才有落点）

⚠ **§4 那张表只有第一行（「分数 = 本局精准度」）属于 P3**，其余四行（等级/称号的棋力分 · 20 位棋手榜按精准度铺 · Stage 连关×倍率 · 每日一题）都是**元游戏件 → P5**。
⭐ P3 欠 P5 的只有一样东西：**一个可信的精准度数字**（Task 1 + Task 6）。P5 拿到它就能把那四行全部接上，⛔ P3 别提前造榜单和称号。
